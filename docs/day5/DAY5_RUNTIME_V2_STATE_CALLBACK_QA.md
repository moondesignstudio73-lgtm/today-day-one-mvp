# DAY 5 V2 런타임 상태·조건부 반응·콜백 QA

상태: **PASS — V2 상태/선택/콜백 구현**  
최우선 원본: Notion `AI해커톤 > day 5` 하위 페이지 본문 및 `DAY5_SCENARIO_REBUILD_V2.md` 합성 계약  
다음 관문: V1 원문 문장 단위 런타임 충실도 대조

## 구현 결과

- SCENE 01과 SCENE 08에 하은 관계 LOW/MID/HIGH 대사를 적용했다. 모든 단계에서 넥타이 접촉 동의와 자발적 보고 원칙을 유지한다.
- 네 선택에 snake_case 정규 키, 단계별 체크포인트, `day5ChoiceEffectsApplied` 중복 방지 계약을 추가했다.
- 선택 1의 권한/관계도/의료 경계 플래그, 선택 2의 서진 기본 프로필, 선택 3의 업무 습관·민호 출처 존중, 선택 4의 복귀 계획·선택별 후속 행동을 저장한다.
- `work_bounded_review`는 `work +3`, `energy -2`를 정확히 적용한다.
- 선택 1+선택 3 조합이 다음 방문 계획표 내레이션에 반영되고, 선택 2가 퇴근 직전 서진 콜백으로 재생된다.
- DAY REPORT 직전 하은 자율성 신뢰와 `day6_life_restart_pending` 후속 훅을 저장한다.
- 저장 복원 시 같은 선택 효과를 다시 적용하지 않는다.

## 검증

- LOW/MID/HIGH 대사 분기 3종
- 네 선택의 레거시 ID와 정규 키 병존
- 서진 AFFECTION/STATUS_INTEREST 독립 및 중복 적용 차단
- 업무 수치·체력 비용, 선택별 해금, 여덟 공통 플래그
- 8 Scene·4 CG·4 전용 배경과 기존 stage 0~4 복원 회귀

문장 단위 원고 충실도와 실제 브라우저 플레이는 아직 최종 PASS가 아니다. DAY 6은 시작하지 않는다.
