# DAY 10 시나리오 자체 QA V1

- 검수 대상: `docs/day10/DAY10_SCENARIO_DRAFT_V1.md`
- 런타임 대상: `src/day10-campaign-runtime.mjs`
- 검수일: 2026-08-26
- 결과: **SCENARIO QA PASS**

## 구조·밀도

- `PASS`: 8개 Scene이 출근 준비 → 권한 확인 → 리듬 선택 → DAY 9 절차 회수 → 현재형 점심 → 마지막 블록 → 세 칸 기록 → 귀가로 진행한다.
- `PASS`: 리듬·점심·귀가 기록의 3개 전략 선택과 27개 경로를 제공한다.
- `PASS`: 장소·목표·대화 상대·선택이 짧은 간격으로 바뀌며 반복 대사나 긴 Pause 없이 8~12분 밀도를 지원한다.

## 연속성·지식 장부

- `PASS`: DAY 9의 범위·압박·피드백 9개 선택을 자료·행동·제한 형식으로 각각 콜백한다.
- `PASS`: 주인공은 시간·증상·자료 출처·현재 책임자·승인 금지를 확인하고 성과를 이유로 범위를 자동 확대하지 않는다.
- `PASS`: 원래 휴대폰을 회수했다고 확정하지 않으며 현재 연락은 임시 예비폰으로 유지한다.
- `PASS`: DAY 11 훅은 범죄 단서가 아닌 현재 생활표다.

## Voice·관계

- `PASS`: 하은은 23세의 밝고 생활적인 연인으로 일정표·저녁·설거지 농담을 주도하며 회사나 위치를 통제하지 않는다.
- `PASS`: 민호는 기억 시험 없는 농담, 서진은 목적 있는 질문, 팀장은 시간·권한 중심의 문장을 쓴다.
- `PASS`: 서진 `AFFECTION`과 `STATUS_INTEREST`를 새로 올리거나 같은 의미로 합치지 않는다.
- `PASS`: 주인공은 관찰 → 가능성 → 확인 → 판단 → 행동 순서로 경계를 실제 행동에 옮긴다.

## 선택·효과·저장

- `PASS`: 세 선택은 모두 행동 전략이며 한 가지 정답으로 수렴하지 않는다.
- `PASS`: 선택 직후 `day10RuntimeStage` 1·2·3과 개별 전략을 저장하고, 낮 카페를 포함한 대응 프레젠테이션으로 복원한다.
- `PASS`: 27개 조합이 `three-hour-work-rhythm-record`와 `day11-current-life-plan`에 도달한다.
- `PASS`: 기존 DAY 9 선택과 하은·서진 관계 상태를 덮어쓰지 않는다.

## 시각 연속성·미스터리 안전

- `PASS`: 점심은 `neighborhood-cafe-day`를 사용해 오전~늦은 오후 시간대와 일치한다. 기존 저녁·비 카페 배경은 제거했다.
- `PASS`: DAY 6~10 생활 확장 공개 예산을 지키며 사고 고의성·가해자·동승 여부·가짜/진짜 하은·잠금 프로필·D-DAY를 차단한다.
- `PASS`: 하은이나 서진을 수상한 표정·거짓말·비밀 행동으로 반전 코딩하지 않는다.

## 남은 출시 관문

- 기존 에셋 원본 해상도·시간대·인물 적합성 감사와 Scene별 연출·오디오 매핑.
- 27개 경로 효과·중복·저장 복원 집중 회귀와 실제 브라우저 연속 플레이 QA.

최종 판정: **SCENARIO QA PASS / NEEDS FIX 0**
