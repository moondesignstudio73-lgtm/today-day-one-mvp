# 《결혼까지 30일!》 DAY 2 실제 게임 구현 보고

상태: `PHASE 20 COMPLETE / PHASE 21 PLAYTHROUGH QA READY`

## 구현 범위

- `src/day2-campaign-runtime.mjs`에 잠금된 12개 Scene을 7개 재개 가능 구간으로 연결했다.
- 결혼 판단, 집 관리 경계, 차량 이동, 커플사진, 연락처 선택과 작은 열쇠 대응을 고유 플래그·즉시 반응·관계/조사 상태로 저장한다.
- 주인공 방은 5개 중 조사하지 않은 항목만 표시하고 정확히 3개가 완료된 뒤에만 Scene 10으로 진행한다. 중복 선택은 거부한다.
- DAY 1 접촉 3종과 질문 3종을 재활 지원, 사고 기록, 가족사진 대사와 행동에 조건부로 회수한다.
- 기존 `m30-day2-rehabilitation`의 완료 기록은 잠금 DAY 2 엔딩이 끝난 뒤 한 번만 기록해 기존 DAY 3 선행 조건과 저장 호환성을 유지한다.

## 프레젠테이션 연결

- 승인 배경 8종, 하은 투명 포즈, 1인칭 행동 레이어, 의료진, 이벤트 CG 4종을 실제 큐에 연결했다.
- 병원 복도·로비·차량·집 앰비언스와 행동 SFX, BGM 음량 곡선을 Beat 진입 시 재생하고 공간 전환 시 이전 루프를 정지한다.
- `D-29`, 사고 세부, 잠금 프로필, 후반 반전은 런타임 문자열과 이미지 큐에 추가하지 않았다.
- 작은 열쇠는 전용 공포 효과 없이 서랍 소리와 `미분류 물건` 판정만 사용하고, 원래 휴대폰과 임시 예비폰을 구분한다.

## 저장·복원과 기존 모드

- `day2RuntimeStage`, 각 전략 플래그, 조사 목록, 작은 열쇠 대응, 연락처 해금을 `storyFlags`에 저장한다.
- 선택 직후 저장은 다음 재개 구간부터 시작하며, 조사 수치·관계 수치·완료 기록을 중복 적용하지 않는다.
- DAY 2 전용 분기는 잠금 Scene ID에만 적용되고 다른 스토리와 자유 연애 모드는 기존 공용 시퀀스를 계속 사용한다.

## 검증

- `tests/day2-runtime.test.mjs`: 12개 Scene 밀도, 모든 선택군, 조건부 선택, 5중 3 탐색, 중복 거부, 에셋 실재, 오디오·게임 연결을 검사한다.
- `tests/day2-audio.test.mjs`, `tests/day1-runtime.test.mjs`, `tests/day2-scenario-draft.test.mjs`가 통과했다.
- `game.js`, DAY 2 런타임·오디오 모듈 문법 검사와 `tests/simulation.test.mjs` 전체 회귀가 통과했다.

PHASE 20 판정: **PASS — NEEDS FIX 0**. 다음 관문은 PHASE 21 처음부터 끝까지 실제 플레이 QA다.
