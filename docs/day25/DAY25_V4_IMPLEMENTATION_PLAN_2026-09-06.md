# DAY25 V4 구현 계획 — 2026-09-06

## 판정과 원문

- 현재 상태: **PARTIAL / SOURCE LOCKED**.
- 최종 원문: Notion `DAY 25 — 좋아한다는 말 다음 | SCENARIO V4`.
- page id: `3c9c31f0-29a6-81c0-93a3-d109c07f6995`.
- last edited snapshot: `2026-08-27T20:46:24.113Z`.
- 플레이어 공개 snapshot: `docs/scenarios/DAY25_SCENARIO_V4_NOTION.md`, UTF-8 17,818자, 24 Scene, 20 choice block. 선택 10은 4개 라벨이고 나머지는 각 3개 라벨이다.
- `INTERNAL EDITORIAL NOTES`, 구현 설명, 대체된 구버전은 플레이어 공개 source와 renderer에서 제외한다.

## 한 문장 정의

좋아한다는 말 뒤에 두 사람이 원하는 집·일·돈·거리·결혼의 의미가 다를 수 있음을 확인하고, 합의된 만큼만 다음 생활 대화와 공개 범위로 옮기는 날.

## 기존 구현 감사

현재 `m30-day25-current-wedding-scope`는 법적 절차·예식·비용·초대 범위를 정하는 4단계 요약이다. 최종 V4의 제목, DAY24 관계 결과별 진입, 24개 장면, 하은/혼자/새 만남 경로, 집·일·돈·거리 대화, 현재 접촉 동의, 친구 식사와 공개 범위에 맞지 않는다. legacy 진행 저장은 보존하고 검증된 DAY24 V4 완료 저장만 신규 schema로 진입시킨다.

## DAY25 INPUT — DAY22~24 실제 이력 감사

- `day24V4.complete === true`와 `day24V4Day25HookPending === true`를 신규 진입의 필수 조건으로 둔다.
- DAY24 `day25Route`를 다시 추론하지 않고 `HAEUN_FUTURE | DEFERRED_RELATIONSHIP | RELATIONSHIP_ENDED | NO_CONVERSATION` 그대로 동결한다.
- 하은 대면 미래 경로는 `haeunRelationshipOutcome === CONTINUE`와 `futureTalkAccepted === true`가 모두 실제일 때만 연다. 약속한 시간·방식이 있으면 그대로 잇는다.
- 유예는 관계 지속이나 종료로 자동 변환하지 않는다. 합의된 재논의만 열고 결혼·동거·키스·친구 공개를 선행시키지 않는다.
- 관계 종료/비접촉은 혼자 하는 미래 이야기로 간다. DAY24 `newMeetingAccepted === true`인 경우에만 SCENE19 새 사람 약속을 열며 새 연애로 확정하지 않는다.
- DAY24의 연락 대상·방향·응답·관계 상태 거짓말과 정정 여부를 그대로 전달한다. 새로운 수신자나 자동 폭로·용서·파국을 만들지 않는다.
- DAY22~23의 여행, 숙소, 사진, 작별 접촉은 회상 사실일 뿐 결혼·동거 합의가 아니다.
- 현재 돈·부채·직업·건강·사생활 의무를 지우지 않으며 미래 소득이나 호감도로 해결하지 않는다.

## DAY25 OUTPUT

- 오늘 대화 장소와 실제 만남 성사 여부.
- 결혼/장기연애/재논의/종료 등 현재 미래 입장의 주인공 제안과 하은의 독립 응답.
- 원하는 집, 혼자 있을 방, 생활비·부채·의무, 일과 귀가 시간, 장거리 가능성, 결정 속도.
- 함께 알아볼 다음 항목과 재논의 시점. Cloud Dining은 후보 관심만 남기고 예약·결제·계약을 만들지 않는다.
- 첫 현재 입맞춤은 활성 관계·양쪽 현재 동의·충분한 신뢰가 모두 있을 때만 별도 응답으로 기록한다. 결혼 동의의 보상으로 사용하지 않는다.
- 친구 식사는 관계 지속, 양쪽 공개 동의, 지훈·소라의 실제 가능 상태가 모두 있을 때만 연다.
- 공개 범위는 실제 합의 수준만 기록하고 예식장·날짜·약혼·결혼 완료를 확정하지 않는다.
- DAY26에는 실제 공개 범위, 미해결 생활 조건, 재논의 약속, DAY24 거짓말 책임을 전달한다.

## Scene Graph

```text
DAY24 완료 이력 → SCENE01~03 / C1~3 공통 도입
  ├─ 상호 관계 지속 + 미래 대화 수락
  │    → SCENE04~16 / C4~13 집·일·돈·거리·속도·현재 접촉
  │    → 조건 충족 시 SCENE20~22 / C14~16 친구 식사·공개 범위
  │    → SCENE23~24 ending
  ├─ 유예/재논의
  │    → 합의된 대화 범위만 수행, 자동 결혼·접촉 없음
  │    → SCENE17~18 대체 C4~6 → SCENE23~24
  └─ 종료/비접촉
       → SCENE17~18 대체 C4~6
       ├─ DAY24 실제 새 만남 수락 → SCENE19 대체 C7
       └─ 그 외 새 사람 없음
       → SCENE23~24 ending
```

## 핵심 불변식

- 주인공의 결혼 희망은 제안이지 하은의 동의가 아니다.
- 하은이 결혼을 원해도 결혼 없는 장기연애를 자동 수락하지 않는다.
- 돈·일·사생활은 장애물이 아니라 함께 조정할 생활 조건이다.
- 현재 접촉 동의 없는 손잡기·포옹·입맞춤을 재생하지 않는다.
- 키스는 관계/결혼 선택 보상이 아니며 거절·포옹·바라보기·작별도 완전한 결과다.
- 친구의 존재·시간·초대 동의와 커플의 공개 동의를 만들지 않는다.
- 편집 메모와 `~경로에서는`, `~인 경우` 같은 분기 설명을 일반 대화창에 출력하지 않는다.
- Story Choice 진행 중 legacy DAY25 Free Action을 열지 않는다.
- 신규 CG를 연결하기 전에 `docs/STORY_V4_IMAGE_STYLE_RULES.md`를 적용한다. 캐릭터와 큰 POV 손의 동시 구도는 기본 FAIL이며, 손이 비필수면 소품·잔류 동작·SFX로 대체한다.

## 구현 단계와 완료 관문

1. ~~Notion 최종 원문을 플레이어 공개 본문과 내부 메모로 분리해 source snapshot을 잠근다.~~ 완료.
2. ~~24 Scene·20 choice block·선택 10의 4개와 나머지 각 3개 라벨을 기계 생성 source registry와 SHA 검증으로 고정한다.~~ 완료.
3. ~~DAY22~24 실제 이력을 동결하는 `day25-notion-v4/1` replay-locked 상태 계약과 legacy 진입 분리를 구현한다.~~ 완료. DAY24 route·관계 결과·미래 대화 수락·새 만남·거짓말, 친구 인지, 현재 잔액·최근 지출과 source schema를 input seal로 고정한다.
4. 공통 도입과 하은 미래 대화, 유예/혼자/새 사람 대체 경로를 exact source ref로 구현한다.
   - ~~공통 SCENE01~03/C1~3와 하은 SCENE04~10/C4~9~~ 완료. 특별식당·집은 별도 현재 장소 응답을 기다리고 예약·결제를 자동 생성하지 않는다.
   - 남은 범위: SCENE11~16/C10~13 하은 응답·현재 접촉, SCENE17~19 혼자/새 사람 대체 경로.
5. 집·일·돈·거리·속도와 하은 독립 응답, 현재 접촉 동의를 구현한다.
6. 친구 가능성·공개 범위·ending과 DAY26 hook을 구현한다.
7. game bridge, 저장 재개, 시간·장소·인물 presentation, Story/Free 배타성을 연결한다.
8. source/state/bridge/저장/경제/전체 30일 시뮬레이션 회귀를 통과한다.
9. Friendly/Neutral/Distant/Mixed와 하은/유예/혼자/새 만남/입맞춤 거절 및 수락 경로를 데스크톱과 389×844에서 SKIP 없이 검증한다.
10. 원문·런타임 텍스트·콘솔·이미지·오디오·오버플로·DAY26 전환이 모두 PASS일 때만 DAY25 COMPLETE로 승격한다.

## 다음 시작점

SCENE11~16/C10~13을 구현해 주인공의 제안과 하은의 현재 독립 응답, 이어지는 관계 상태, 손잡기·포옹·입맞춤의 현재 동의를 exact source로 분리한다. 그 다음 SCENE17~19 혼자/새 사람 대체 경로를 연결한다. 이 관문 전까지 DAY25는 **PARTIAL**이다.
