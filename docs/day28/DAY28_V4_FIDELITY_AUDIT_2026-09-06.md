# DAY28 연결 후 재감사

판정: **PARTIAL / 보완 필요**. 기존 단계별 완료 기록은 파일·기능 단위이며 원문 전체 충실도나 실제 네 경로 플레이 완료를 뜻하지 않는다.

## 이번 수정

- `startImmersiveScene`의 DAY28 재개 처리에서 legacy 리허설 presentation을 호출하던 잔여 코드를 V4/legacy 분기 함수로 변경했다. V4 배경 URL을 보존하고 등장 인물이 없는 장면은 이전 캐릭터 자산도 해제한다.
- 이전 DAY 테스트 7개 파일의 게임 캐시 버전 검증은 277 이상을 허용하도록 수정했다. 후속 DAY의 캐시 증가만으로 과거 DAY 테스트가 실패하던 문제다.

## 다음 구현에서 먼저 해결할 HIGH 문제

1. `day28-v4-runtime-resolution.mjs`는 관계 응답을 주인공의 희망에 맞춰 반환하고, 대부분의 접촉을 자동 수락한다. 응답 API를 분리했다는 것만으로 상대의 독립 반응이 구현된 것은 아니다. 실제 선행 사실에 따른 현재 NPC 반응과 거절/유예를 구현하고 원문 대사로 보여야 한다.
2. CALL 경로도 SCENE03 이후 산책·벤치·대면 인물과 접촉으로 이어질 수 있다. 통화용 장면/선택과 presentation을 분리해야 한다.
3. C3의 `lieCorrected`만으로 하은이 거짓을 들었다고 판단할 수 없다. 실제 거짓 존재·대상·하은 고지·청취를 함께 확인해야 한다.
4. 여러 `playerNarration`이 원문 줄을 참조하지만 실제 대사와 행동을 요약하고 있다. source ref 유효성 검사는 원문 전체를 플레이했다는 증거가 아니다. SCENE03~16 선택별 원문 대사·행동과 SCENE20~21 선택 결과를 보강해야 한다.
5. C11 작별 뒤 일상 대화로 계속 진행하는 흐름, 종료 희망과 상대 응답이 충돌하는 경우, 새 상대에게 하은 전용 C16을 제공할 가능성을 재검사해야 한다.

이 문제를 보완한 다음 데스크톱·389×844 Friendly/Neutral/Distant/Mixed 비-SKIP QA와 저장 재개를 수행한다. 현재 DAY29 원본 구현으로 넘어가지 않는다.

## 후속: 명시적 종료의 새 응답 보호

- C8 `END_WELL` 뒤 새 `CONTINUE/KNOW_AGAIN` 응답 적용을 상태 API에서 거부한다. 상대의 지속 희망이 주인공의 관계 종료 결정을 덮지 않으며, 실패 시 chapter/history/pending을 보존한다.
- SCENE08의 명시적 이별 응답과 SCENE09의 `응`, `언젠가 좋게 생각할 수 있으면 좋겠어. 지금 약속은 못 하지만.`을 원문 그대로 복원했다. 잠깐의 침묵은 지원되는 `storyPause`로 처리한다.
- replay reducer는 변경하지 않아 과거 유효 저장의 검증 규칙은 보존한다. 이미 과거 기록에 모순된 응답이 저장된 경우의 의미 교정은 별도 호환성 작업이며, 이번 변경이 과거 기록까지 해결했다는 뜻은 아니다.
- 종료 선택→응답 거부의 원자성 및 종료 수락→원문 대사→JSON 저장 왕복을 자동 검증했다. 실제 브라우저 네 경로는 아직 NOT RUN이다.
- 캐시는 game280 → DAY28 bridge v2 → state/middle v2다.

다음 시작점: C11 GOODBYE 뒤 SCENE14~15를 건너뛰는 흐름을 기존 `day28-notion-v4/1` 저장을 손상시키지 않는 방식으로 설계·구현한다. 이후 CALL 장면 분리, 실제 고지/청취, 독립 NPC 응답과 원문 누락 보완을 이어간다. DAY28은 PARTIAL이다.

## 후속: C11 직접 작별과 저장 호환성

- 새 C11 `GOODBYE` 선택에는 `flow: DIRECT_FAREWELL`을 기록하고 `daily_listening`·집 초대를 건너뛰어 SCENE16 작별로 직접 이동한다. 선택 직후 원문의 `아쉬운 마음이 남아도 오늘의 거리를 지킬 수 있었다.`를 먼저 재생한다.
- 이 경로에서 SCENE14 양말 대화, C12 일상 청취, 집 초대 cue, SCENE15 집 기억이 노출되지 않으며 `dailyListening`과 `homeInvitationResponse`도 생성하지 않는다.
- 수정 전 저장의 C11 기록에는 flow 표식이 없다. replay 시 표식 없는 기록만 기존 `daily_listening` 전이를 사용하므로 유효한 중간 저장을 무효화하거나 자동 변경하지 않는다. 새 저장은 직접 작별 전이를 그대로 replay한다.
- 직접 작별과 구저장 호환성, 중간 저장/브리지 회귀를 집중 검증했다. 실제 브라우저는 NOT RUN이다.
- 캐시는 game281 → DAY28 bridge v3 → state/middle v3다.

다음 시작점: CALL 경로에서 산책·벤치·대면 접촉 선택과 인물을 제거하고 통화에 맞는 원문 대사/종료 presentation으로 분리한다. 이후 실제 고지/청취, 독립 NPC 응답과 남은 원문 대사·행동을 보완한다. DAY28은 계속 PARTIAL이다.

## 후속: CALL 전용 흐름

- 새로 수락된 하은 CALL resolution에 `flow: CALL_CONVERSATION`을 기록한다. 수정 전 표식 없는 CALL 저장은 기존 replay 전이를 유지해 저장을 무효화하지 않는다.
- CALL C2는 원문의 전화 대체 지시에 따라 `지금 이야기할 여유가 있어?`를 표시하고, 화면에 보이지 않는 표정·몸짓·하은 스프라이트를 만들지 않는다. C4의 `같이 걷는다` 선택과 SCENE06~07의 길·벤치·먼지 장면을 제거한다.
- C5 뒤 SCENE08 관계 대화로 이동한다. 지속/다시 알아가기 응답 뒤 SCENE11~16의 손잡기·포옹·키스·집 초대를 전부 건너뛰며, SCENE22는 캐릭터 스프라이트 대신 통화 cue와 실제 관계 응답을 표시한다. C16 완료 뒤 통화 종료 cue를 낸다.
- 일반 준비를 고른 대면 약속은 체력이 낮다는 이유만으로 CALL로 자동 변경하지 않는다. C1 `SHORT`와 낮은 체력이 함께 있을 때만 현재 기본 resolver가 짧은 통화를 선택한다.
- 새 CALL 분리, 수정 전 저장 호환성, 대면/통화 선택 조건을 집중 검증했고 전체 Node 859/859 PASS다. 실제 브라우저는 NOT RUN이다.
- 캐시는 game282 → DAY28 bridge v4 → state/middle v4, opening/ending v2, runtime-resolution v2다.

다음 시작점: C3 `hidden_fact`가 실제 거짓 존재·수신자·하은 고지·청취를 모두 요구하도록 DAY25~27 입력 근거를 재감사한다. 그 뒤 NPC의 독립 현재 응답과 남은 원문 대사·행동을 보완한다. DAY28은 PARTIAL이다.

## 후속: C3 거짓 고지·청취 증거

- DAY27의 `lieCorrected`는 거짓의 실제 수신자에게 정정했다는 뜻이며 하은이 들었다는 뜻이 아니다. DAY28 신규 입력에 `haeunTruthKnowledge`를 추가해 실제 거짓 수신자, 정정 여부, 하은 고지 선택, 하은의 현재 청취 응답을 함께 봉인한다.
- C3 `hidden_fact`는 거짓이 실제 존재하고 정정됐으며, DAY27에서 `DISCLOSE` 또는 `REPORT_CORRECTION`을 골랐고 하은이 `LISTEN`으로 답한 경우에만 노출한다. 고지 숨김, 청취 유예/종료, 거짓 없음에는 해당 선택을 열지 않는다.
- proof 내부의 `heard` 값을 고지·청취 기록과 다르게 조작하면 seal을 다시 계산해도 검증을 통과하지 못한다.
- 수정 전 DAY28 저장에는 proof 필드가 없다. 해당 저장은 당시 선택 기록을 계속 replay할 수 있도록 기존 `lieCorrected` 판정을 유지하며 자동 재작성하지 않는다.
- 실제 SEOJIN 거짓 정정→하은 고지→LISTEN 이력과 고지 숨김 대조를 포함해 전체 Node 861/861 PASS다. 실제 브라우저는 NOT RUN이다.
- 캐시는 game283 → DAY28 bridge/state v5, opening/ending v3, middle v5다.

다음 시작점: `day28-v4-runtime-resolution.mjs`의 관계·접촉·새 관계 응답이 주인공 희망을 그대로 수락하는 문제를 선행 사실 기반 독립 NPC 응답으로 바꾸고, 응답별 원문 대사를 표시한다. DAY28은 PARTIAL이다.

## 후속: 독립 NPC 현재 응답과 대사

- 하은 관계 응답은 C8 선택을 복사하지 않는다. 명시적 종료·거리 요청은 `END`, 지속 희망은 기존 신뢰가 충분할 때만 `CONTINUE`, 그 외에는 `KNOW_AGAIN`으로 답한다. 결과는 SCENE09 또는 SCENE11 첫 대사로 직접 들린다.
- 현재 접촉은 동작별 조건을 분리했다. 손은 연락 허용과 거리 존중, 포옹은 신뢰·지속 응답에 따라 수락 또는 `오늘은 손만`, 키스는 신뢰·지속 응답·오늘 실제 손/포옹이 모두 있어야 수락한다. 수락·부분 수락·거절은 다음 장면 전에 하은 대사로 표시한다.
- 집 초대는 지속 응답·신뢰·오늘 접촉이 모두 있는 경우만 기본 resolver가 수락한다. 다음 만남도 지속 관계는 수락, 다시 알아가기는 유예, 종료는 거절로 분리했다.
- 새 관계는 `WANT_ROMANCE`와 이전 상대의 `RECIPROCATE`, 거짓 정리, 반복 거짓 없음이 모두 있을 때만 현재 상호 응답으로 이어진다. 아라는 `시간이 맞을 때 다시 만나요.`처럼 실제 상대별 대사를 말하며 SCENE19도 하은 전용 설명을 재사용하지 않는다.
- 응답 분기 보강으로 처음 실제 진입한 SCENE15의 잘못 잘린 source ref 두 곳도 전체 원문 줄로 교정했다.
- 독립 관계·손/포옹/키스·새 상대 대사와 기존 브리지 경로를 포함해 전체 Node 866/866 PASS다. 실제 브라우저는 NOT RUN이다.
- 캐시는 game284 → DAY28 bridge v6, middle v6, ending v4, runtime-resolution v3다.

다음 시작점: SCENE03~16과 SCENE20~21의 `playerNarration` 요약을 선택 결과별 실제 원문 대사·행동으로 확장하고 player-facing 문자열 전수 감사를 수행한다. DAY28은 PARTIAL이다.

## 후속: SCENE03~16·20~21 실제 대화와 행동 확장

- SCENE03~07은 C2 긴장 응답, 하은의 고쳐 말하기, 상처 종류에 따른 현재 답, 서로의 작은 약속, 짧은 길 대화와 벤치 농담을 실제 `dialogue`로 복원했다. 대면 행동은 `storyActionCue`로 분리해 일반 대화창에 지문을 넣지 않으며 CALL에서는 보이지 않는 몸짓을 만들지 않는다.
- SCENE04는 모든 경로에 큰 상처용 `보고 싶었어 / 다시 괜찮은 건 다르고`를 공통 투영하지 않는다. 실제 거짓·거리 요청·반복 거짓이 없는 경로에는 원문의 피로와 비회피 대사를 사용한다.
- SCENE11은 고쳐 말해도 되는 관계에 관한 원문 대화를 복원했다. SCENE13은 실제 전날 청취 근거가 충분할 때만 `어제 내 말 끝까지 들은 거`를 말하고, 그 외에는 원문의 오늘 청취 표현을 사용한다. 키스 수락 뒤 `응. 그럼 좋았어.`까지 재생한다.
- SCENE14는 양말 농담, 일 이야기 선택, 짧은 업무 답장, `내가 지금 할 수 있는 건`, 대화로 돌아오는 질문까지 원문 대사와 행동으로 연결했다. 집 초대 응답은 이 대화 뒤에만 요청된다.
- SCENE15~16은 실제 집 초대 수락 때만 입장·컵·머무름 행동을 만들고, C13 결과와 최종 `다시 만나자는 게 전으로 돌아가자는 말은 아니네`를 재생한다. 직접 작별과 외부 종료는 집 장면을 계속 건너뛴다.
- SCENE20~21은 일곱 선택 각각에 산책, 개인 일, 감정 유지, 식사, 컵 씻기/음악/휴식, 필요한 연락, 내일 준비, 오늘 답 보존 중 하나의 실제 행동 또는 1인칭 독백을 붙였다. 개발 메모형 문장을 visible narration으로 승격하지 않는다.
- 집중 24/24, 전체 Node 869/869, `node --check game.js`, `git diff --check` PASS다. 실제 브라우저 비-SKIP QA는 이번 실행에서 NOT RUN이다.
- 캐시는 game285 → DAY28 bridge v7 → opening v4, middle v7, ending v5다.

다음 시작점: SCENE17~19와 SCENE22~24의 남은 요약 투영을 실제 상대별 대사·행동으로 확장하고 전체 player-facing 문자열을 원문과 대조한다. 그 뒤 데스크톱 Friendly부터 비-SKIP 브라우저 QA를 시작한다. DAY28은 계속 PARTIAL이다.
