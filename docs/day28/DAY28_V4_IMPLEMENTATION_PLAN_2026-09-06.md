# DAY28 V4 구현 계획 — 2026-09-06

## 판정과 원문

- 현재 상태: **PARTIAL / SOURCE LOCKED**.
- 최종 원문: Notion `DAY 28 — 다시 만나자는 뜻 | SCENARIO V4`.
- page id: `3c9c31f0-29a6-81ab-aeb5-f17aaa4072d8`.
- last edited snapshot: `2026-08-27T21:01:47.079Z`.
- 플레이어 공개 snapshot: `docs/scenarios/DAY28_SCENARIO_V4_NOTION.md`, 마지막 LF 포함 UTF-8 16,541자, 24 Scene, 26 choice block, SHA-256 `7e108834a0aa31361f5c22a2d2a69e7127e3b3ff8cd622727438ecbfe94b6d5b`.
- source registry variant: `COMMON_MORNING`, `HAEUN_MEETING`, `BREAKUP_CLOSE`, `HAEUN_CONTINUING`, `NEW_MEETING`, `SOCIAL_SCOPE`, `SOLO_LIFE`, `CONTINUING_NIGHT`.
- 내부 편집 메모와 대체 V3는 snapshot과 renderer에서 제외한다.

## 한 문장 정의

다시 만남을 과거 복원으로 오해하지 않고, 상대가 실제로 한 말과 현재 가능한 거리를 들고 다음 관계를 새로 고르는 날.

## 기존 구현 감사

현재 `src/day28-campaign-runtime.mjs`는 집에서 예식 동선과 중단 신호를 시험하는 5장면·3선택 요약형 legacy 구현이다. 최종 V4의 재회·정리 대화·새 만남·혼자 생활, 현재 접촉 동의, 실제 집 초대와 공개 범위를 구현하지 않는다. 기존 DAY28 진행 저장은 legacy로 보존하고, 검증된 DAY27 V4 완료 저장만 신규 schema로 진입시킨다. V4가 DAY29에 넘기는 것은 실제 관계·거리·생활 계획이며, legacy 예식 리허설 완료를 자동 생성하지 않는다.

## DAY28 INPUT — DAY25~27 실제 이력 감사

- `day25V4`, `day26V4`, `day27V4`가 모두 유효·완료되고 DAY27의 DAY28 hook이 있을 때만 신규 DAY28을 연다.
- DAY27 `day28Handoff`의 관계 지속, 연락 허용, 실제 대화 상대, 거짓말 정정/반복, 공개 정정, 상대가 수락한 다음 대화, 거리 요청을 그대로 동결한다.
- DAY25의 미래 합의(`PREPARE_MARRIAGE`, `REDISCUSS`, `LONG_DATING`, `END`)는 DAY28의 만남 하나로 원래 상태로 복원하지 않는다.
- DAY25의 실제 키스 여부와 DAY27의 현재 신뢰 경로를 분리한다. 과거 키스가 있어도 오늘 접촉은 다시 독립 응답을 기다린다.
- DAY26의 단일 새 만남 상대와 관계 의도·거짓말 정정·다음 만남 응답을 보존한다. `NEED_TIME`, 단순 호감, 재예약은 교제 수락이 아니다.
- DAY27에서 만남/통화가 `ACCEPTED`였거나 다음 대화가 현재 `ACCEPTED`인 상대만 SCENE02 또는 SCENE17에 등장한다. 시간 요청·거절·연락 중지에는 집 앞 방문이나 자동 재회가 없다.
- 실제 맡긴 물건, 실제 집 초대 이력, 지훈에게 공유한 범위가 없으면 물건 반환·집 내부 기억·친구 보고를 만들지 않는다.
- 현재 에너지·스트레스·잔액을 보존하고 선물·카페·교통 비용을 자동 생성하지 않는다.

## DAY28 OUTPUT

- 실제 만남 상대·방식·현재 성립 응답과 상대가 고른 짧은 길/대화 범위.
- 하은과의 관계는 지속·다시 알아가기·종료를 주인공 선택과 하은의 독립 응답으로 분리한다.
- 손잡기·포옹·키스·집 초대는 각각 현재 동의를 기록하며 거절·손만·대화만도 온전한 결과다.
- 이별은 실제 반환 물건, 사진 사생활, 필요한 연락 범위만 기록한다.
- 새 만남은 단일 실제 상대의 독립 응답이 `RECIPROCATE`일 때만 새 연애가 될 수 있고, `NEED_TIME`·거절은 그대로 유지한다.
- 친구에게는 함께 정한 관계 이름과 범위만 말하고 상대의 사적인 내용은 저장하지 않는다.
- 혼자 경로는 식사·휴식·필요 연락·내일 한 가지를 남기되 상대 응답을 만들지 않는다.
- DAY29에는 실제 관계 상태, 연락/거리, 현재 접촉, 집 초대, 공유 범위, 의자·옷 정리처럼 실제 합의한 생활 계획만 넘긴다.

## Scene Graph

```text
DAY27 완료 이력 → SCENE01 / C1
  ├─ 하은과 실제 만남·통화 성립 → SCENE02~08 / C2~8
  │    ├─ 관계 종료 합의 → SCENE09~10 / 이별 C9~10
  │    └─ 관계 지속·다시 알아가기 → SCENE11~16 / 지속 C9~14
  │         ├─ 현재 접촉 동의·신뢰 충분 → SCENE13 / C11
  │         └─ 현재 집 초대 쌍방 수락 → SCENE14~15 / C12~13
  ├─ 실제 새 만남 성립 → SCENE17~18 / 새 만남 C8
  └─ 만남 없음·정리 후 혼자 → SCENE20~21 / 대체 C2~8
→ 실제 공유 대상이 있으면 SCENE19 / C15
→ 관계 지속·연락 허용이면 SCENE22 / C16
→ SCENE23~24 → DAY29 handoff
```

## 핵심 불변식

- `보고 싶다`는 말과 관계 복원·용서·결혼 준비 재개를 같은 값으로 취급하지 않는다.
- 현재 상대의 명확한 답 없이 만남, 접촉, 집 초대, 다음 약속, 새 연애를 만들지 않는다.
- 한 번의 산책·사과·웃음·키스로 큰 신뢰 손상을 복구하지 않는다.
- 하은이 모르는 거짓말을 알고 용서한 것처럼 말하지 않는다.
- 새 상대에게 하은의 대사·생활 계획·집 기억을 이름만 바꿔 투영하지 않는다.
- 관계 종료는 상대의 기억 보관 방식과 연락 중지 권리를 보존한다.
- 선택 분기 설명과 편집 메모를 일반 대화창에 출력하지 않는다.
- 신규 CG는 `docs/STORY_V4_IMAGE_STYLE_RULES.md`를 통과해야 하며 큰 POV 손이나 실사형 손을 만들지 않는다.

## 구현 단계와 완료 관문

1. ~~Notion 최종 원문을 플레이어 공개 snapshot으로 잠그고 내부 편집 메모·대체 V3를 분리한다.~~ 완료.
2. ~~24 Scene·26 choice block·8개 variant와 모든 라벨을 기계 생성 registry 및 SHA 검증으로 고정한다.~~ 완료.
3. ~~DAY25~27 실제 이력을 동결하는 `day28-notion-v4/1` replay-locked 상태 계약과 legacy 진입 분리를 구현한다.~~ 완료.
4. ~~SCENE01~08/C1~8 실제 재회·고쳐 듣기·관계 제안 playable을 구현한다.~~ 완료.
5. ~~SCENE09~16 이별 C9~10, 지속 C9~14, 현재 접촉·집 초대 응답을 구현한다.~~ 완료.
6. ~~SCENE17~24 새 만남 C8·공개 C15·혼자 C2~8·밤 C16·ending을 구현한다.~~ 완료.
7. ~~game bridge, 저장 재개, 현재 응답, presentation, Story/Free 배타성을 연결한다.~~ 완료.
8. source/state/playable/bridge/저장/전체 30일 회귀를 통과한다.
9. Friendly/Neutral/Distant/Mixed를 데스크톱과 실제 콘텐츠 389×844에서 SKIP 없이 검증한다.
10. 원문·런타임 텍스트·콘솔·자산·오버플로·DAY29 전환이 모두 PASS일 때만 DAY28 COMPLETE로 승격한다.

## 다음 시작점

`DAY28_V4_FIDELITY_AUDIT_2026-09-06.md`에 기록한 HIGH 문제부터 보완한다. 특히 현재 NPC 응답, CALL 분리, 하은의 실제 고지/청취, 원문 대사·행동 누락을 해결한 뒤 브라우저 QA로 진행한다. 위 단계의 완료 표시는 원문 전체 충실도 PASS를 뜻하지 않는다.

C8 명시적 종료에 대한 새 응답 덮어쓰기 방지와 SCENE08~09 이별 대사 복원은 보완했다. 다음 구현은 C11 GOODBYE 뒤 일상/집 장면 우회와 기존 저장 호환성부터 시작한다. 이전 저장 기록 자체는 변경하지 않았다.

C11 GOODBYE는 새 저장에서 SCENE14~15를 건너뛰고 SCENE16으로 직접 이동한다. 수정 전 표식 없는 저장은 기존 replay 경로를 유지해 유효성을 보존한다. 다음 구현은 CALL 전용 장면·선택·presentation 분리다.

새 CALL은 통화용 C2와 C3~5·C8만 사용하고 SCENE06~07 및 SCENE11~16의 대면 행동을 건너뛴다. 캐릭터 스프라이트 없이 통화 cue로 표시하며 수정 전 CALL 저장은 기존 replay를 유지한다. 다음은 C3 거짓 고지·청취 근거 감사다.

C3 `hidden_fact`는 신규 입력의 실제 거짓·수신자·정정·하은 고지·LISTEN proof가 모두 맞을 때만 제공한다. 수정 전 proof 없는 저장은 기존 replay를 유지한다. 다음은 독립 NPC 현재 응답과 원문 응답 대사 구현이다.
