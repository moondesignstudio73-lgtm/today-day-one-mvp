# DAY18 실제 QA 및 대화 기록 시간 수정

2026-09-04 20:24 KST 시작. DAY18 PARTIAL 유지.

## 실제 브라우저 증거

- 별도 QA fixture, 혼자 식사 → 다른 김밥 → 지훈에게 안부 → 바로 귀가 → 밤 선택까지 SKIP 없이 진행했다. 타자 완료 클릭을 사용했으므로 자연 독서시간이나 전체 성향 완주 검증이 아니다.
- 지훈의 짧은 문자 세 줄이 읽기 도우미 출력에서 빠졌지만 실제 LOG에는 모두 존재했다. 도우미 누락을 게임 대사 누락으로 오판하지 않았다.
- 사진 없이 “리뷰가 아니라 증거네”가 이어지는 원인 누락은 여전히 존재한다. 사진 이벤트 미완료 항목을 유지한다.
- 새 오류 발견: 저녁 19:00 장면을 보고 있는데 LOG의 모든 대사가 MORNING · 아침으로 기록됨. Story presentation 시간과 Free Mode의 `state.phase`가 독립적인데 LOG가 후자만 읽고 있었다.

## 수정 및 검증

- 대화 삽입 시점의 Story presentation 시간/시각을 기록하도록 변경했다. Free Mode의 기존 시간 표시는 유지하며 상태·저장 스키마는 바꾸지 않는다.
- 수정된 런타임을 새 탭에서 기존 QA 저녁 저장으로 재개했다. 지훈 문자와 식사 독백에 `저녁 · 19:00` 표시 확인.
- 바로 귀가 후 밤 도입은 `밤 · 22:00`, 앞선 저녁 기록은 `저녁 · 19:00`으로 남는 것을 실제 LOG에서 확인했다.
- 해당 탭 콘솔 오류/경고 없음. 테스트 전 저장은 최초 fixture 탭의 복원 버튼으로 복구하고 성공 문구를 확인했다.
- 전체 자동 테스트 432 PASS, 0 FAIL. `node --check game.js`, `git diff --check` 통과. 캐시 버전 186으로 갱신했다.
- DAY18 전체 완주/4성향/모바일/물건 연출은 이번 검증 범위가 아니다.

## 다음 시작점

SCENE15 빈 그릇 사진 이벤트와 SCENE14 옆자리 이동 연출/순서 수정이 여전히 우선이다. 전화/문자 구분, 원문 물건 행동과 4성향 완주도 남았다. 이번 LOG 수정으로 이들 항목을 PASS 처리하지 않는다.
