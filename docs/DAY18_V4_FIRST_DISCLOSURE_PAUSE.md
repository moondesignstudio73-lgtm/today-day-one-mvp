# DAY18 SCENE02 — 처음 알리는 답장의 간격

2026-09-05 01:52 실행, game217. DAY18 PARTIAL.

원문의 “처음 전한다면 하은은 바로 답하지 않았다”가 실제 재생에서 빠져 있었다. 유리 저녁을 처음 고지하는 `disclose_yuri`에서만 첫 하은 답장 “언제 정했어?” 앞에 기존 `storyPause` 1200ms를 추가했다. 이미 알고 있는 약속에는 같은 망설임을 반복하지 않는다. 가짜 '입력 중' 문자나 미확인 읽음 상태는 만들지 않는다.

입력 계약은 기존 `haeunKnowsAppointment`, 출력/schema/문자 내용 변경 없음. 짧은 연출 간격이며 현실의 정확한 대기시간을 뜻하지 않는다. 기존 reduced-motion 설정에서는 pause가 0으로 처리되는 엔진 정책을 그대로 따른다. 숨겨진 원문 문장을 독백으로 바꾸지 않는다.

신규 자동 검사는 3개 schema×기존 고지 여부에서 pause 위치/첫 화자/무문자 이벤트/상태 순수성/JSON 재생을 확인한다. 실제 브라우저의 새 pause 체감 및 SKIP/재개는 이번 실행에서 NOT RUN이며 다음 확인 대상이다.

전체 자동 테스트 472 PASS, 0 FAIL. `node --check game.js`, `git diff --check` 통과.

SCENE02 단어를 쓰고 지우는 행동과 다른 잔여 행동, 알람 입력·소리, 최종 동일 버전 4경로/모바일 및 DAY15~17 감사는 아직 남았다. DAY19로 넘어가지 않는다.

## 01:56 실제 재생 후속

game217에서 하은에게 약속을 아직 알리지 않은 유리 경로를 아침부터 SKIP 없이 진행했다. 유지→점심 선택→처음 고지 직후에는 아직 이전 점심 질문이 남아 있고, 이후 별도 입력 없이 하은의 “언제 정했어?”로 진행했다. 가짜 입력 중/읽음 문자는 표시되지 않았다. 화면은 12:30을 유지했다. 관찰 시점 사이에 Scene 전환도 포함되므로 1200ms의 정확한 실측 증거로 주장하지 않는다.

새로고침/이어하기에서 같은 하은 첫 답장으로 재개 확인. 콘솔 오류/경고 없음. 같은 QA 탭에서 사용자 저장 복원 완료. SKIP/모바일/기고지 비교의 실제 검증까지 완료한 것은 아니다. 다음은 SCENE02 ‘저녁’ 단어 작성/삭제를 전송되지 않은 초안으로 표현하는 연출이며, 새 연락 이력을 만들지 않아야 한다.
