# DAY 1 실제 게임 구현 보고서

상태: `PHASE 20 — PASS`

## 구현 범위

- SCENARIO LOCK의 6개 Scene과 두 선택 시점을 `src/day1-campaign-runtime.mjs`에 플레이 가능한 세그먼트로 구현했다.
- 첫 접촉 3전략과 정보 확인 3전략을 모두 저장하며, 첫 선택은 기존 DAY 2 콜백 ID와 호환되도록 매핑했다.
- 병실 배경 2종, 하은 표정·포즈, 의료진 2개 레이어, CG 3종과 PHASE 19 오디오 큐를 실제 런타임에 연결했다.
- 선택 전·후 저장 복원 시 완료한 선택을 반복 적용하지 않고 해당 세그먼트에서 재개한다.
- 캠페인 DAY 1에서만 전용 런타임을 사용하며 자유 연애 모드의 기존 장면 생성 경로는 유지한다.

## 자동 검증

- 3×3 선택 조합 9개에서 선택 기억, 기존 콜백 ID, 상태 효과, 저장·불러오기와 DAY 1 중복 방지를 검증했다.
- 실제 시퀀스의 승인 이미지 경로, CG 3종, 60개 이상 대사 턴과 금지 정보 미노출을 검증했다.
- `node --check game.js`, `node --check src/story-data.mjs`: 통과.
- `tests/day1-runtime.test.mjs`, `tests/day1-audio.test.mjs`, `tests/simulation.test.mjs`: 통과.

## 로컬 실제 플레이 확인

- 캠페인 선택 → 플레이어 설정 → 프롤로그 건너뛰기 → DAY 1 자동 진입: PASS.
- 첫 접촉 선택 3개 표시와 `contact_boundary` 즉시 반응: PASS.
- 75개 진행 입력 후 정보 확인 선택 3개 표시: PASS.
- `accident_interest` 선택 뒤 105개 진행 입력으로 결말과 기본 행동 화면 복귀: PASS.
- 실행 중 브라우저 콘솔 오류: 0개.

## PHASE 21 이관

PHASE 21에서는 모든 3×3 경로를 실제 화면으로 처음부터 끝까지 재생하고, 모바일 크롭·키보드 포커스·스킵·중간 저장 복원·CG 최소 표시·오디오 음량을 별도 QA한다. PHASE 20은 실제 연결 완료이며 최종 품질 승인을 의미하지 않는다.
