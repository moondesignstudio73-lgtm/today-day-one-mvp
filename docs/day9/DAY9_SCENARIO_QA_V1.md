# DAY 9 시나리오 자체 QA V1

- 검수 대상: `docs/day9/DAY9_SCENARIO_DRAFT_V1.md`
- 검수일: 2026-08-26
- 결과: **SCENARIO QA PASS**

## 구조·밀도

- `PASS`: 8개 Scene이 아침 준비 → 혼자 출근 → 현재 팀 재진입 → 적응 범위 선택 → 권한 밖 압박 → 책임 분리 → 서진 피드백 → 90분 종료·귀가로 진행한다.
- `PASS`: 3개 전략 선택, 27개 DAY 9 경로, DAY 5·8 콜백을 제공한다.
- `PASS`: 장소·참여자·목표·압박·선택이 30~90초 단위로 바뀌며 반복 대사나 긴 Pause 없이 9~13분 밀도를 지원한다.

## 연속성·지식 장부

- `PASS`: DAY 8의 연락·구매·귀가 공유 9개 선택을 출근 준비·보고 형식·업무 경계로 콜백한다.
- `PASS`: DAY 5의 최종 복귀 전략 3종과 서진 확인 전략을 현재 자료 제시·말투에 반영하되 과거 선택을 덮어쓰지 않는다.
- `PASS`: 주인공은 현재 권한·책임자·자료 출처를 확인하고 과거 평판만으로 결정하지 않는다.
- `PASS`: 민호·서진·팀장은 공개된 의료 제한과 현재 회사 정보만 사용한다. 하은은 회사 내부·서진의 관심을 모른다.

## Voice·관계

- `PASS`: 하은은 밝고 생활적이며 출근을 통제하거나 서진을 질투하지 않는다.
- `PASS`: 민호는 친밀도를 강요하지 않는 현재형 농담, 서진은 목적 있는 질문, 팀장은 시간·권한 중심의 문장을 사용한다.
- `PASS`: 서진 `AFFECTION` 우세·`STATUS_INTEREST` 우세·둘 다 높음·둘 다 낮음의 관계별 말투가 실제 질문 순서와 내용에서 달라진다.
- `PASS`: `office9_debrief_name_limits`는 AFFECTION만, `office9_debrief_write_protocol`은 STATUS_INTEREST만, `office9_debrief_targeted_feedback`은 두 축을 각각 소폭 바꾼다.

## 선택·효과·저장

- `PASS`: 적응 범위·권한 압박·피드백 계약은 서로 다른 행동 전략이며 단순 호감 버튼이 아니다.
- `PASS`: 선택 직후 4단계 저장 복원, 구버전 stage 0, 선택 기억, 해금·훅 중복 방지를 명시했다.
- `PASS`: 27개 선택 조합이 모두 안전한 90분 종료와 DAY 10 훅에 도달한다.

## 미스터리·안전

- `PASS`: DAY 6~10 생활 확장 공개 예산을 지키며 범죄·정체 단서를 추가하지 않는다.
- `PASS`: 사고 고의성·가해자·동승 여부, 가짜/진짜 하은, 하은 잠금 프로필, D-DAY 수치를 플레이 대사에서 차단한다.
- `PASS`: 급한 질문은 회복 중인 주인공을 영웅화하거나 의료 제한을 무시하지 않고 현재 책임자에게 연결된다.

## 남은 구현 관문

- 기존 에셋 감사와 8개 Scene 연출·오디오 매핑.
- 3단계 런타임, 선택별 서진 양축 효과, 저장 복원, DAY 10 전환 구현.
- 27개 경로 집중 회귀와 실제 브라우저 QA.

최종 판정: **SCENARIO QA PASS / NEEDS FIX 0**

