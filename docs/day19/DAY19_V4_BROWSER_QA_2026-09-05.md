# DAY 19 V4 실제 브라우저 QA

## QA 계약

- 실제 `index.html`과 본선 `game.js`를 로컬 서버에서 실행한다.
- 경로 fixture는 테스트 시작 전 Story/Free/공용 저장 키를 세션에 한 번 백업하고 종료 뒤 명시적으로 복원한다.
- SKIP 버튼은 사용하지 않는다. 화면에 나타난 모든 대화·문자·독백을 진행하고 각 선택을 실제 버튼으로 누른다.
- Friendly/Neutral/Distant/Mixed는 별도 실행으로 검증하며 자동 순수 함수 검사를 실제 플레이 증거로 합산하지 않는다.

## Friendly — PASS

- 입력: DAY18 평온한 하은 저녁, 여행 대화 완료, 연락 가능, 미완료 제3자 연락 없음.
- 실제 선택: 하은의 하루 묻기 → 짧은 여행 비교 → 오락비 지출 없음 → 민호 날짜 확인 → 각자 편한 예산 → 장소 하나 제거 → 좋은 하루 → 조건만 남김 → 이동 편의 → 부산 1박 후보 → 후보 유지 → 오늘 남은 돈 → 내일 집 식사 제안 → 컵 준비 → 생활비 보존.
- SCENE 01~24와 15개 실제 선택(C15 조건부 생략)을 SKIP 없이 진행하고 DAY20 선택 화면 도달.
- C4 뒤 SCENE06, C7 뒤 SCENE12~13, C12 뒤 SCENE18 연결이 끊기지 않음.
- C5 선택 뒤 새로고침/이어하기 시 선택 반응부터 다시 재생되어 C6으로 정상 복귀.
- 09:00 → 14:00 → 19:00 → 22:00 시간과 집/카페/집 배경 전환 확인.
- C11 이후 예약·결제 완료 표현 없음. C15는 실제 미완료 연락이 없어 화면에 나타나지 않음.
- DAY20 진입 화면에서 `storyState=STORY_CHOICE`, Story 선택 표시, Free Action 숨김.
- 데스크톱 실제 viewport `1484×799`, `scrollWidth=1484`: 가로 넘침 0.
- 브라우저 console warning/error 0.
- 테스트 종료 뒤 fixture의 `테스트 전 저장 복원` 상태 문구 확인.

## Distant — PASS (game249)

- 입력: 하은 연락 휴식, solo 계획, 실제 미완료 제3자 연락 없음, 사전 오락비 없음.
- 첫 실플레이에서 C8에 공동 결제/추가 요구, C11에 `둘 다 확실히`가 노출되는 충실도 결함을 발견했다. 상태 replay 계약은 유지해 기존 저장을 깨지 않되 실제 presentation에서 연락 불가 C8은 `각자 하루`, solo C11은 `후보로 둔다`, solo C16은 생활비/추가 지출 중단만 노출하도록 fail-closed 했다.
- 수정 후 실제 선택: 현재 돈 확인 → 가까운 하루 → 지출 없음 → 민호에게 내일 답변 → 생활비 우선 → 보고 싶은 하나 → 좋은 하루를 준비한 마음 → 각자 하루 → 돈을 남김 → 여행 미룸 → 후보로 둠 → 오늘 남은 돈 → 각자 저녁 → 내 컵 → 추가 지출 중단.
- C1 하은 문의, shared C5, 공동 C8, 두 사람 C11, 함께 먹기/내일 초대 C13, shared C14, C15, 합의 금액 C16은 화면에 나타나지 않았다.
- DAY20 선택 화면까지 SKIP 없이 완주했다. 전 구간 하은 인물 스프라이트 노출 0, console warning/error 0.
- DAY20에서 `storyState=STORY_CHOICE`, Story 선택 표시, Free Action 숨김. 데스크톱 `1484×799`, `scrollWidth=1484`: 가로 넘침 0.
- 테스트 종료 뒤 `테스트 전 저장을 복원했습니다.`를 확인했다.
- presentation 필터 회귀는 기존 replay 옵션 세 개를 그대로 인정해 공개 직전 저장 호환성을 보존한다.

## Mixed — PASS (game249)

- 입력: 하은 연락 가능/solo 계획, DAY18 밤 연락을 내일로 미룬 실제 이력, DAY12 서진 개인 초대·원칙적 수락 이력, 사전 오락비 있음.
- 최초 Mixed fixture는 연락 가능 DAY18 경로에서 disclosure와 night 선택을 생략해 `menu_familiar`, `alone_stop`이 거부됐다. 사용자 저장을 변경하기 전에 멈췄고, `disclose_solo → night_defer → alone_stop`을 포함한 합법 replay로 교정했다.
- 교정 뒤 새 클린 탭에서 실제 선택: 하은의 하루 문의 → 짧은 여행 비교 → 오락비 안 복권 1회 → 민호에게 예상 시간 문의 → 개인 우선 지출 → 늦은 출발 → 관계를 편하게 하고 싶은 마음 → 서로 원하는 것 추가 → 풍경 → 부산 1박 후보 → 후보 유지 → 아쉬움 인정 → 내일 집 식사 제안 → 혼자 먹을 것 하나 → 서진 연락에 시간 요청 → 생활비 보존.
- 내일 식사 제안은 shared 수락으로 승격되지 않고 solo C14가 표시됐다. C15는 실제 서진 미완료 연락 때문에 나타났으며, 화면에는 존재하지 않는 유리·아라 이름을 넣지 않았다. solo C11은 후보 유지 하나, solo C16은 합의 금액 없이 두 선택만 노출됐다.
- 실제 UI에서 복권 선택은 한 번만 눌렀고 결과·등급·상금 표현은 끝까지 나타나지 않았다. 정확한 5,000원 1회 원장 반영과 `prizeCounted:false`는 브리지 자동 원자성 검사로 별도 확인했다. Story 화면의 숨겨진 자산 카드는 진행 중 갱신 증거로 사용하지 않았다.
- 16개 선택을 SKIP 없이 DAY20까지 완주했다. 전 구간 대면 인물 스프라이트 노출 0, console warning/error 0.
- DAY20에서 `storyState=STORY_CHOICE`, Story 선택 표시, Free Action 숨김. 데스크톱 `1484×799`, `scrollWidth=1484`: 가로 넘침 0.
- 클린 탭 테스트 종료 뒤 `테스트 전 저장을 복원했습니다.`를 확인했다.

## Neutral — PASS (game250)

- 입력: DAY18 평온한 하은 저녁, 여행 대화 완료, 연락 가능, 미완료 제3자 연락 없음.
- 첫 실행에서 C8 `각자 하루` 뒤에도 C11 `둘 다 확실히`가 노출되고 SCENE22가 편안한 공동 여행 통화를 재생할 수 있는 원문 경계 오류를 발견했다. 사용자 저장을 복원하고, 기존 replay 선택은 유지한 채 `SEPARATE_DAY_OK` presentation의 C11을 후보 유지 하나로 제한하고 SCENE22를 거리 두기 원문으로 연결했다.
- game250 재실행 실제 선택: 현재 돈 → 멀리 가는 건 보류 → 투자 분리 → 민호에게 내일 답변 → 돈을 숨기려는 선택의 거절 반응 → 늦은 출발 → 해 줄 수 있음을 보이고 싶은 마음 → 각자 여행 → 돈을 남김 → 서울 하루 후보 → 후보 유지 → 현재 돈 → 오늘 각자 저녁 → 내일 몸을 보고 혼자 식사 결정 → 합의된 범위만 기록.
- C8 이후 C11에는 `오늘은 후보로 두자.`만 표시됐다. C13 각자 저녁 뒤 내일 집 식사를 제안하지 않아 C14는 solo 세 선택으로 열렸고 C15는 실제 미완료 연락이 없어 생략됐다.
- C16 shared 범위는 C5에서 실제 함께 본 예산 범위에만 한정했고 하은 돈을 합산하거나 송금하지 않았다.
- SCENE22에서 `나는 그 대화를 상상해서 하은이 답한 것처럼 만들지 않았다.`를 실제 확인했고 `처음 본 방, 아직 생각나?` 공동 통화는 나타나지 않았다. SCENE24는 다음 이야기를 하은의 시간도 보고 정하는 거리 두기 인사로 끝났다.
- 15개 선택을 SKIP 없이 DAY20까지 완주했다. console warning/error 0, DAY20 `storyState=STORY_CHOICE`, Story 선택 표시, Free Action 숨김.
- 데스크톱 `1484×799`, `scrollWidth=1484`: 가로 넘침 0. 종료 뒤 `테스트 전 저장을 복원했습니다.` 확인.

## 남은 경로

- 없음. 데스크톱·모바일 Friendly/Neutral/Distant/Mixed와 사용자 저장 복원을 모두 완료했다.

## Mobile Friendly — PASS (game250)

- viewport capability의 목표값을 390×844에 맞췄다. 브라우저 장치 배율 반올림으로 실제 CSS viewport는 `391×844`였고 모바일 미디어쿼리는 활성화됐다.
- 데스크톱 Friendly와 같은 15개 실제 선택(C15 조건부 생략)을 모두 화면 버튼으로 눌렀다. SKIP은 사용하지 않았다.
- SCENE01부터 Friendly 공동 계획, 후보만 유지, 내일 집 식사 수락, 생활비 보존 결말을 지나 DAY20 첫 선택 화면까지 도달했다.
- 각 선택 경계에서 수평 넘침을 검사했으며 최대값은 0이었다. 종료 화면은 `clientWidth=391`, `scrollWidth=391`, stage 폭 `390.8046`이었다.
- DAY20의 세 Story 선택이 접근성 트리에 표시됐고 Free Action 조작은 나타나지 않았다. console warning/error는 0이었다.
- 종료 뒤 fixture에서 `테스트 전 저장을 복원했습니다.`를 확인하고 임시 viewport를 기본 `1103×620`으로 되돌렸다.

## Mobile Distant — PASS (game251)

- 연락 휴식/solo 입력으로 실제 CSS viewport `389×844`와 모바일 미디어쿼리를 확인했다. 15개 실제 선택(C15 조건부 생략)을 모두 화면 버튼으로 눌렀고 SKIP은 사용하지 않았다.
- DAY19 전 구간에 하은 인물·대사 노출이 없었고, 각자 여행·후보 유지·각자 저녁·내 컵·추가 지출 중단으로 끝났다. 선택 경계 최대 수평 넘침은 0이었다.
- 첫 완주에서 내일 식사를 수락하지 않았는데도 완료 이력만 보고 기존 `m30-day20-current-shared-meal`이 열려 하은의 방문과 공동 식사를 만든 DAY19→20 경계 결함을 발견했다.
- 완료 브리지는 `tomorrowMeal === 'ACCEPTED'`인 경우에만 `day20CurrentSharedMealPending`을 세우고, 기존 DAY20 공동 식사 장면은 이 표식까지 요구하도록 수정했다. 비수락 Distant 재완주에서는 공동 식사 대사 `좋아. 나 진짜 내 취향으로 가져간다.`, 질문 `오늘 집에서 무엇을 준비할까?`, 관련 선택이 모두 나타나지 않았다.
- exact DAY20 V4가 아직 구현되지 않았으므로 Distant의 DAY20은 `다음 장면을 준비하고 있습니다.`에서 fail-closed 한다. 이는 DAY20 구현 완료 판정이 아니며, 존재하지 않는 하은 방문을 만드는 것보다 안전한 경계다.
- 종료 화면도 `clientWidth=scrollWidth=389`였고 console warning/error 0이었다. fixture에서 `테스트 전 저장을 복원했습니다.`를 확인한 뒤 viewport를 기본 `1103×620`으로 되돌렸다.

## Mobile Mixed — PASS (game251)

- 하은 연락 가능/solo 계획, DAY18 밤 연락 유보, DAY12 서진의 실제 미완료 연락, 사전 오락비가 있는 Mixed 입력을 실제 CSS viewport `389×844`와 모바일 미디어쿼리에서 검증했다.
- 하은의 하루 문의 → 짧은 여행 비교 → 오락비 안 복권 1회 → 민호 예상 시간 문의 → 개인 우선 지출 → 늦은 출발 → 관계를 편하게 하고 싶은 마음 → 서로 원하는 것 추가 → 풍경 → 부산 1박 후보 → 후보 유지 → 아쉬움 인정 → 내일 집 식사 제안 → 혼자 먹을 것 하나 → 서진에게 시간 요청 → 생활비 보존의 16개 선택을 모두 화면 버튼으로 눌렀다. SKIP은 사용하지 않았다.
- 내일 식사 제안은 수락으로 승격되지 않아 solo C14가 열렸고, C15는 실제 이어 온 서진 연락 때문에 나타났다. 존재하지 않는 유리·아라 연락, 예약·결제, 복권 결과·등급·상금 표현은 만들지 않았다.
- 전 구간 대면 인물 스프라이트 노출 0, 선택 경계 최대 수평 넘침 0, 종료 `clientWidth=scrollWidth=389`, console warning/error 0이었다.
- DAY20에서는 잘못된 하은 방문 대사·공동 식사 질문/선택 없이 `다음 장면을 준비하고 있습니다.`로 fail-closed 했다. exact DAY20 V4 완료 증거로 사용하지 않는다.
- fixture에서 `테스트 전 저장을 복원했습니다.`를 확인하고 임시 viewport를 기본 `1103×620`으로 되돌렸다.

## Mobile Neutral — PASS (game251)

- 평온한 하은 저녁/여행 대화 완료/연락 가능/미완료 제3자 연락 없음 입력을 실제 CSS viewport `389×844`와 모바일 미디어쿼리에서 검증했다.
- 현재 돈 → 장거리 보류 → 투자 분리 → 민호에게 내일 답변 → 돈 걱정을 숨기려는 제안과 하은의 거절 → 늦은 출발 → 해 줄 수 있음을 보이고 싶은 마음 → 각자 여행 → 돈을 남김 → 서울 하루 후보 → 후보 유지 → 현재 돈 → 각자 저녁 → 내일 몸을 보고 혼자 식사 결정 → 합의된 범위만 기록의 15개 선택(C15 생략)을 모두 실제 버튼으로 눌렀다. SKIP은 사용하지 않았다.
- C8 이후 C11은 후보 유지 하나만 표시됐다. C13 이후 solo C14가 열렸고 C15는 실제 미완료 연락이 없어 생략됐다. C16은 하은 돈을 합산·송금하지 않았다.
- SCENE22 거리 두기 문장 `나는 그 대화를 상상해서 하은이 답한 것처럼 만들지 않았다.`를 확인했고, 금지된 공동 통화 `처음 본 방, 아직 생각나?`는 나타나지 않았다.
- 전 구간 대면 인물 스프라이트 노출 0, 선택 경계 최대 수평 넘침 0, 종료 `clientWidth=scrollWidth=389`, console warning/error 0이었다.
- DAY20은 잘못된 하은 방문·공동 식사 없이 placeholder에서 fail-closed 했다. fixture에서 사용자 저장을 복원하고 viewport를 기본 `1103×620`으로 되돌렸다.

## 최종 판정

DAY 19 V4는 **PASS / COMPLETE**다. 24개 원본 Scene과 16개 Choice의 조건부 재생, 후보≠예약, 복권 원자성, 중간 저장 재개, Story/Free 배타성, 데스크톱·모바일 네 경로 비-SKIP 완주, 콘솔 오류 0, 사용자 저장 복원을 완료했다. DAY20 원본 구현은 별도이며 다음 시작점은 **DAY20 최종 Notion 원문 잠금과 DAY17~19 실제 이력 감사**다.
