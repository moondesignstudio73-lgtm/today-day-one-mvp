# DAY18 V4 4성향 실제 경로 최종 QA

## 판정

**PASS / COMPLETE.** game248에서 Friendly, Neutral, Distant, Mixed 네 경로의 데스크톱·모바일 비-SKIP 완주, 유리/하은/혼자 대표 Scene-start 저장 재개, DAY15~17 충실도 재감사를 모두 닫았다. DAY18 완료 조건을 충족했으며 다음 순차 구현 대상은 DAY19다.

## Friendly — PASS (2026-09-05)

- 진입: DAY17 완료 fixture의 `하은과 저녁`, 실제 `불러오기`로 DAY18 시작.
- 조작: SKIP 버튼을 한 번도 사용하지 않고 대화창 클릭, 알람 정지, 모든 선택지를 직접 선택했다.
- 주요 선택 흐름: 하은과 약속 유지 → 함께 저녁 → `오늘 너랑 이렇게 있는 게 좋아.` → `옆으로 가도 돼?` → `오늘 너랑 먹어서 좋았어.` → `하루 어디 다녀오고 싶어.` → `가까운 데서 하루 보내는 걸 보자.`
- 연출 확인: 아침 입력, 하은 저녁 19:00, 옆자리 동의 뒤 거리 변화, 22:00 문자, 평온한 밤 대화, 방 행동과 최종 암전 뒤 DAY19 금요일 전환.
- 상태 경계: 다른 사람에게 실제 관심이 없는 fixture에서 다른 마음을 발언하거나 존재하지 않는 연락·합의를 생성하지 않았다.
- 결과: DAY19 진입 PASS, 브라우저 콘솔 error/warn 0.
- 저장: fixture가 만든 QA 저장을 테스트 전 사용자 저장으로 복원했다.

## Mixed — PASS (2026-09-05)

- 진입: DAY17 완료 fixture의 `유리와 저녁`에 `하은에게 유리 약속을 아직 말하지 않음` 입력을 켠 뒤 실제 `불러오기`로 DAY18 시작.
- 조작: SKIP 버튼 없이 대화창 클릭, 알람 정지, 12개 선택지를 직접 선택했다.
- 선택 흐름: 약속 유지 → 상대 미고지 → 각자 메뉴 → 현재의 유리에게 관심 → 과거 외로움 공감 → 하은과 현재 관계 명시 → 재만남 요청 → 각자 결제 → 하은에게 사실 보고 → 내일 가능한 시간 다시 묻기 → 개인 메모 → 부산 시간·비용 후보.
- 상태 경계: 유리의 재만남 답변은 `REQUESTED_NOT_ACCEPTED`로 유지됐고, 하은과 통화 시각 합의·여행 동의를 생성하지 않았다. 개인 메모는 전송되지 않았다.
- 연출 확인: 유리 식당/야외 작별, 22:00 미고지 사실 보고, 통화 연기, 혼자 방과 개인 메모, 여행 사진·부산 후보, 암전 뒤 DAY19 전환.
- 결과: DAY19 진입 PASS, 브라우저 콘솔 error/warn 0.
- 저장: fixture가 만든 QA 저장을 테스트 전 사용자 저장으로 복원했다.

## 저장 재개 — 유리 저녁 PASS (2026-09-05)

- 진입: 결제 후 야외 작별 집중 fixture를 실제 `불러오기`로 시작했다.
- 새로고침 전: `yuri-outerwear-close` 겉옷 여밈 뒤 `유리: 잘 들어가.` → `나: 유리 씨도요.` → 인물 숨김 순서를 SKIP 없이 확인했다.
- 저장 재개: 작별 진행 중 페이지를 새로고침한 뒤 실제 `불러오기`를 눌러 같은 Scene 시작부터 재생되는 계약을 확인했다. 겉옷 여밈, 두 작별 대사와 인물 표시가 같은 순서로 복구됐다.
- 범위: 정확한 대사 오프셋 복원이 아니라 프로젝트가 채택한 Scene-start 복구 계약에 대한 PASS다.
- 결과: 브라우저 콘솔 error/warn 0, fixture 종료 후 테스트 전 사용자 저장 복원 PASS.

## 저장 재개 — 하은 밤 PASS (2026-09-05)

- 진입: 연락 가능한 밤 통화 집중 fixture를 실제 `불러오기`로 시작했다.
- 새로고침 전: 22:00 사실 보고 문자 → 하은의 통화 가능 응답 → 통화 전환 → `당분간 각자 지내 보자고 했어.` → 침묵 뒤 내적 반응 → 하은 응답 순서를 SKIP 없이 확인했다.
- 저장 재개: 하은 응답 진행 중 페이지를 새로고침한 뒤 실제 `불러오기`를 눌렀다. 같은 Scene 시작의 사실 보고 문자부터 통화 가능 응답, 현재의 유리에 대한 설명과 `당분간 각자 지내 보자고 했어.`까지 동일한 순서로 복구됐다.
- 범위: 정확한 대사 오프셋 복원이 아니라 프로젝트가 채택한 Scene-start 복구 계약에 대한 PASS다.
- 결과: 브라우저 콘솔 error/warn 0, fixture 종료 후 테스트 전 사용자 저장 복원 PASS.

## 저장 재개 — 혼자 밤 PASS (2026-09-05)

- 진입: 하은 연락 휴식이 적용된 여행 사진 집중 fixture를 실제 `불러오기`로 시작했다.
- 새로고침 전: 컵 정리 독백 → `SCENE 21 · 여행 사진 한 장` → 사진이 미래를 정하지 않는다는 내적 반응 → 내일 살펴볼 하루 세 후보 선택 순서를 SKIP 없이 확인했다.
- 저장 재개: 세 후보 선택 중 페이지를 새로고침한 뒤 실제 `불러오기`를 눌렀다. 같은 Scene 시작의 컵 정리 독백부터 여행 사진 장면, 같은 내적 반응과 세 후보 선택까지 동일 순서로 복구됐다.
- 상태 경계: 연락 휴식 입력을 유지해 하은의 문자·통화나 존재하지 않는 여행 합의를 생성하지 않았다.
- 범위: 정확한 선택지 오프셋 복원이 아니라 프로젝트가 채택한 Scene-start 복구 계약에 대한 PASS다.
- 결과: 브라우저 콘솔 error/warn 0, fixture 종료 후 테스트 전 사용자 저장 복원 PASS.

## 네 경로 공통 결과

- game247에서 네 계획 경로 모두 DAY18 아침부터 DAY19까지 SKIP 없이 클린 완주했다.
- 유리 저녁, 하은 밤, 혼자 밤의 대표 Scene-start 저장 재개를 실제 새로고침과 `불러오기`로 모두 확인했다.
- 각 경로는 별도 fixture 저장으로 시작했고 종료 후 매번 사용자 저장을 복원했다.
- 이 결과는 자연 독서 시간이나 NEW GAME→DAY18 통합 완주 증거가 아니다.

## Distant — PASS (2026-09-05)

- 진입: DAY17 완료 fixture의 `혼자 저녁`에 `하은 연락 휴식` 입력을 켠 뒤 실제 `불러오기`로 DAY18 시작.
- 조작: SKIP 버튼 없이 대화창 클릭, 알람 정지, 6개 선택지를 직접 선택했다.
- 선택 흐름: 혼자 저녁 → 익숙한 김밥 → 휴대전화 없이 식사 완료 → 바로 귀가 → 오늘은 더 연락하지 않기 → 현재 생활 후보.
- 연출 확인: 19:00 혼자 식당, 휴대전화/추가 식사/봉투 행동, 바로 귀가, 22:00 혼자 방, 방 행동·암전 뒤 DAY19 전환.
- 상태 경계: 연락 휴식 입력에서 하은 낮 메시지와 밤 연락 선택이 나오지 않았고, 식당·귀가 구간의 `#vnCharacter` 비노출을 확인했다. 갑작스러운 갈등·연락 의무·동행 약속을 만들지 않았다.
- 결과: DAY19 진입 PASS, 브라우저 콘솔 error/warn 0.
- 저장: fixture가 만든 QA 저장을 테스트 전 사용자 저장으로 복원했다.

## Neutral — PASS (2026-09-05)

- 진입: DAY17 완료 fixture의 `유리와 저녁`, 실제 `불러오기`로 DAY18 시작.
- 조작: SKIP 버튼 없이 대화창 클릭, 알람 정지, 11개 선택지를 직접 선택했다.
- 선택 흐름: 약속 유지 → 하은에게 유리 저녁 사전 고지 → 각자 메뉴 → 과거 이야기 → 고마움 → 하은과 현재 관계 명시 → 당분간 시간 두기 → 각자 결제 → 하은에게 사실 보고 → 확신 유보 → 현재 생활 후보.
- 연출 확인: 유리 도착/식당 19:00, 냅킨·컵·시선 행동, 결제와 야외 작별, 22:00 하은 대화, 방 행동·암전 뒤 DAY19 전환.
- 상태 경계: 재만남 요청·무기한 기다림·여행 동의를 생성하지 않았고, 확신 유보 뒤 평온한 여행 통화로 잘못 합류하지 않았다.
- 결과: DAY19 진입 PASS, 브라우저 콘솔 error/warn 0.
- 저장: fixture가 만든 QA 저장을 테스트 전 사용자 저장으로 복원했다.

## 종결 순서

1. 네 경로 모바일 대표 화면과 종료 전환: PASS.
2. DAY15~17 충실도 재감사: 모두 PASS/COMPLETE.
3. DAY18 완료 재판정: PASS/COMPLETE.

## 모바일 최종 비교 — PASS (2026-09-05)

- 브라우저의 전용 viewport capability로 목표 `390×844`를 설정했다. 브라우저 스케일 반올림 뒤 페이지가 측정한 유효 콘텐츠 영역은 `389×844`였고 모바일 미디어쿼리는 `true`였다. 1px 폭 차이는 동일 설정의 렌더링 반올림이며 별도 데스크톱 축소 화면으로 집계하지 않았다.
- Friendly: 하은 저녁 8개 선택을 실제 선택지가 나타난 뒤 직접 눌러 DAY19까지 비-SKIP 완주했다.
- Neutral: 유리 사전 고지·각자 메뉴·과거·고마움·현재 관계·시간 두기·각자 결제·사실 보고·확신 유보·현재 생활의 11개 선택을 DAY19까지 비-SKIP 완주했다.
- Distant: 연락 휴식·혼자 저녁에서 자동 단일 고지 뒤 익숙한 김밥·휴대전화 없이 식사·바로 귀가·연락 중단·현재 생활을 선택해 DAY19까지 비-SKIP 완주했다. 식당과 귀가에 새 인물·연락을 만들지 않았다.
- Mixed: 미고지·현재 관심·외로움 공감·현재 관계·재만남 요청·각자 결제·사실 보고·내일 가능한 시간·미전송 개인 메모·부산 후보를 포함한 12개 선택을 DAY19까지 비-SKIP 완주했다. 재만남 수락·통화 시각 합의·여행 동의를 만들지 않았다.
- 네 경로 모두 `documentElement.scrollWidth === innerWidth`로 가로 넘침이 없었고, `#visualNovelStage`가 유효 폭 전체를 사용했다. DAY19 모바일 화면에서 헤더·시간·인물·대화창을 실제 캡처해 확인했다.
- 경로별 console warning/error는 0이다. 각 경로 사이와 마지막 종료 뒤 fixture의 복원 UI로 사용자 저장을 되돌렸고, 임시 viewport override도 reset했다.
- 종결 전체 회귀에서 `index.html`의 실제 `game.js?v=248`과 맞지 않던 DAY1·DAY14·DAY16의 과거 캐시 기대값 `v=247` 세 건을 발견했다. 제품 코드를 되돌리지 않고 회귀 기대값을 현재 공개 캐시에 맞춘 뒤 전체 테스트를 재실행한다.
- 기대값 교정 후 `node --test --test-reporter=dot tests/*.test.mjs` 종료 코드 0, `npm run check`, 100회×30일 시뮬레이션과 `git diff --check`를 통과했다.
