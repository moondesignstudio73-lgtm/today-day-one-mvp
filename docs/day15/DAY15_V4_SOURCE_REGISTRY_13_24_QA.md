# DAY 15 V4 SCENE 13~24 원문 레지스트리 QA

상태: `EXACT SOURCE REGISTRY 12/12 PASS · CHOICES 8~12 LINKED · LATE ROUTE RESOLVER PASS`

## 최우선 원고

- `AI해커톤 > DAY 15 — 빛나는 쪽을 보다 | SCENARIO V4` 하위 페이지 본문을 `2026-08-31 04:23–04:26 KST`에 새로 완전 조회했다.
- 페이지 ID는 `3c9c31f0-29a6-8138-ab56-ed8ee668526d`, 전체 23,171자 SHA-256은 `c1b542d06e5d931aae5c70f45eb936ae1bdd23efa02d9c9b018282a70b5543ea`로 직전 잠금과 동일하다.
- 상위 Markdown 첨부와 파일 블록은 사용하지 않았다.
- SCENE 13 표제부터 `[DAY 15 END]`까지 정확히 8,508자를 `RAW_DAY15_V4_SCRIPT_13_24`에 보존했다. 이 절편의 SHA-256은 `ea39ac07f3e45fb61092b999720a1575c679c47791834e9d815ce4965cf02658`다.

## 구현 범위

- SCENE 13~24를 안정 ID, 제목, 원문 Markdown, 대화·내레이션·섹션·무대지시의 비필터 `steps`로 분해했다.
- 선택 8~12의 번호와 선택 ID 정본을 `day15-v4-campaign-data.mjs`에 연결했다.
- 대면·통화·무연락, 참석, 하은 이탈, 공개 자료 제안에 따른 장면 진입 자격을 소스 조회기에 연결했다.
- 선택 9에서 하은이 떠난 뒤에도 이미 지난 SCENE 13~16이 복원 조회에서 소급 삭제되지 않도록 과거 장면과 이후 접촉 장면을 분리했다. 손상된 `PHONE + haeunLeft` 상태에서도 SCENE 18과 19가 동시에 활성화되지 않는다.

## SIP 냉독 판정

무맥락 냉독은 이 산출물을 “원문 레지스트리”로 정확히 해석했지만, `steps`가 모든 `###` 대체 경로를 한 배열에 보존하고 선택 목록 줄을 의도적으로 제외하므로 레지스트리 자체는 직접 렌더링 가능한 플레이 런타임이 아니라고 지적했다. 레지스트리 역할은 `verbatim-route-superset`, `unfiltered-source-steps`로 유지하며, `day15-v4-playable-resolver-13-24.mjs`가 실행 시점에 정확히 한 경로를 골라 몰입형 어댑터에 공급한다. 현재 런타임 상태는 `exact-route-resolved-adapter-connected`다.

냉독이 찾은 소급 비활성화와 `PHONE + haeunLeft` 중복 가능성은 코드와 회귀 테스트로 교정했다. 후속 resolver 냉독에서 찾은 누락 마커의 묵시적 절단, 잘못된 선택 ID의 빈 장면, 선택 11 감사 경로의 무출력, 참석+통화 종결의 상충 지식을 모두 fail-closed 검증과 경로별 테스트로 교정했다.

## SSOT·사실 검증

- 후반 원문 절편 정본은 `RAW_DAY15_V4_SCRIPT_13_24`, 선택 버튼·효과 ID 정본은 `day15-v4-campaign-data.mjs`, 저장 상태 유효성 정본은 `day15-v4-state-contract.mjs`다.
- 두 방향 검색에서 같은 후반 원문을 실행 정본으로 주장하는 다른 모듈은 없었다. 문서의 장면 수·상태 표기는 설명용이며 코드 정본을 대체하지 않는다. 승인 없는 통합·이동은 하지 않았다.
- 외부 현실 주장이 아니라 사용자가 쓴 허구 원고와 저장소 내부 계약을 검증하는 작업이므로 `factchk` 외부 조회는 적용 대상이 아니다.

## 검증과 남은 관문

- SCENE 13~24 `12/12`, 선택 번호 8~12 `5/5`, 정확한 절편 길이·SHA, 금지 공개, 장면 진입, 선택 복원, 비접촉 경로와 파서 밀도를 검증했다.
- SCENE 01~12, 후반 레지스트리·resolver, V4 상태 계약·선택 런타임의 집중 합계는 `44 PASS / 0 FAIL`이다.
- 후반 resolver는 SCENE 14·15·16·17·18·20·21·22·24의 상호 배타 분기와 선택 8~12를 상태별 하나만 재생한다. 잘못된 콜백·귀가·선택·종결 조합과 원문 마커 유실은 빈 화면 대신 오류로 차단한다.
- SCENE 01~24 전체의 줄 단위 원고 누락 0과 게임 진입 연결은 아직 주장하지 않는다. 전반부 타입 데이터의 원문 대조, 전후반 단일 프레젠테이션 연결, 체크포인트 복원과 완료 함의를 통합 런타임에서 통과해야 한다.
- DAY 15 커밋·origin·gh-pages·공개 확인 및 DAY 16 시작은 금지 상태를 유지한다.
