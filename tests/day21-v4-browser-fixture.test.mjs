import assert from 'node:assert/strict';import test from 'node:test';import {readFileSync} from 'node:fs';
test('DAY21 browser fixture uses actual prior reducers, backs up saves, and exposes six routes',()=>{const source=readFileSync(new URL('./day21-v4-browser-entry.html',import.meta.url),'utf8');for(const route of ['park','phone','deferred','busan','seoul','rest'])assert.match(source,new RegExp(`data-route="${route}"`));assert.match(source,/beginDay18V4/);assert.match(source,/prepareDay19V4GameEntry/);assert.match(source,/prepareDay20V4GameEntry/);assert.match(source,/completeDay20V4GameChapter/);assert.match(source,/route!==\'deferred\'/);assert.match(source,/qa-day21-busan/);assert.doesNotMatch(source,/partnerCost\s*:/,{message:'fixture must not merge partner cost'});assert.doesNotMatch(source,/partnerCostIncluded:true/);assert.match(source,/sessionStorage\.getItem\(backupKey\)===null/);assert.match(source,/value===null\?localStorage\.removeItem/);});
