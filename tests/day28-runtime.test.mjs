import assert from "node:assert/strict";import { createInitialState } from "../src/game-core.mjs";import { createGirlfriendFromProfile } from "../src/girlfriend-manager.mjs";import { GAME_MODES } from "../src/scenario-state.mjs";import { selectNextStoryScene } from "../src/story-manager.mjs";import { DAY28_ADAPT_CHOICES,DAY28_ROUTE_CHOICES,DAY28_SIGNAL_CHOICES,applyLockedDay28ChoiceState,getLockedDay28ResumePresentation,getLockedDay28Segment,validateLockedDay28Runtime } from "../src/day28-campaign-runtime.mjs";
const s=createInitialState(createGirlfriendFromProfile("haeun",()=>.5),()=>.5,{mode:GAME_MODES.MARRIAGE_30});s.day=28;s.storyHistory=[{sceneId:"m30-day27-current-final-check",choiceId:"final27_execute_only_confirmed",day:27,response:"완료"}];s.storyFlags={day28CurrentCeremonyRehearsalPending:true};s.scenario.featureUnlocks={finance:true,shop:true};assert.equal(selectNextStoryScene(s)?.id,"m30-day28-current-ceremony-rehearsal");assert.equal(validateLockedDay28Runtime(),true);assert.equal(getLockedDay28Segment(s).filter(x=>x.type==="choice").length,1);assert.equal(applyLockedDay28ChoiceState(s,DAY28_ROUTE_CHOICES[0].id).stage,1);assert.equal(getLockedDay28ResumePresentation(s).backgroundId,"home-morning");assert.equal(applyLockedDay28ChoiceState(s,DAY28_SIGNAL_CHOICES[0].id).stage,2);assert.equal(applyLockedDay28ChoiceState(s,DAY28_ADAPT_CHOICES[0].id).stage,3);assert.equal(s.storyFlags.day28CurrentCeremonyRehearsalCompleted,true);assert.equal(s.storyFlags.day29CurrentEveReconfirmationPending,true);assert.equal(s.scenario.featureUnlocks.investment,undefined);for(const id of ["rehearsed-short-rest-exit-route","tested-no-question-stop-signal","rehearsal-reduce-and-buffer-plan"])assert.ok(s.scenario.unlockedActions.includes(id));assert.ok(s.scenario.followUpHooks.includes("day29-current-eve-reconfirmation"));assert.ok(s.scenario.clues.includes("current-ceremony-rehearsal-record"));const restored=structuredClone(s);assert.equal(restored.storyFlags.day28RuntimeStage,3);assert.equal(getLockedDay28Segment(restored).at(-1).type,"sceneEnd");console.log("day28-runtime.test: all assertions passed");
