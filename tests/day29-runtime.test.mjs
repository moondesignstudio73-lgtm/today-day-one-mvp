import assert from "node:assert/strict";import { createInitialState } from "../src/game-core.mjs";import { createGirlfriendFromProfile } from "../src/girlfriend-manager.mjs";import { GAME_MODES } from "../src/scenario-state.mjs";import { selectNextStoryScene } from "../src/story-manager.mjs";import { DAY29_BODY_CHOICES,DAY29_INTENT_CHOICES,DAY29_PLAN_CHOICES,applyLockedDay29ChoiceState,getLockedDay29ResumePresentation,getLockedDay29Segment,validateLockedDay29Runtime } from "../src/day29-campaign-runtime.mjs";
const s=createInitialState(createGirlfriendFromProfile("haeun",()=>.5),()=>.5,{mode:GAME_MODES.MARRIAGE_30});s.day=29;s.storyHistory=[{sceneId:"m30-day28-current-ceremony-rehearsal",choiceId:"rehearsal28_adapt_remove_step",day:28,response:"완료"}];s.storyFlags={day29CurrentEveReconfirmationPending:true};s.scenario.featureUnlocks={finance:true,shop:true};assert.equal(selectNextStoryScene(s)?.id,"m30-day29-current-eve-reconfirmation");assert.equal(validateLockedDay29Runtime(),true);assert.equal(getLockedDay29Segment(s).filter(x=>x.type==="choice").length,1);assert.equal(applyLockedDay29ChoiceState(s,DAY29_INTENT_CHOICES[0].id).stage,1);assert.equal(getLockedDay29ResumePresentation(s).backgroundId,"home-morning");assert.equal(applyLockedDay29ChoiceState(s,DAY29_BODY_CHOICES[0].id).stage,2);assert.equal(applyLockedDay29ChoiceState(s,DAY29_PLAN_CHOICES[0].id).stage,3);assert.equal(s.storyFlags.day29CurrentEveReconfirmationCompleted,true);assert.equal(s.storyFlags.day30CurrentWeddingDecisionPending,true);for(const id of ["independent-eve-mutual-intent","wedding-morning-body-check","proceed-simplify-postpone-equal"])assert.ok(s.scenario.unlockedActions.includes(id));assert.ok(s.scenario.followUpHooks.includes("day30-current-wedding-decision"));assert.ok(s.scenario.clues.includes("current-eve-reconfirmation-record"));const restored=structuredClone(s);assert.equal(restored.storyFlags.day29RuntimeStage,3);assert.equal(getLockedDay29Segment(restored).at(-1).type,"sceneEnd");console.log("day29-runtime.test: all assertions passed");
