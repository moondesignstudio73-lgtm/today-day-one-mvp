import assert from "node:assert/strict";import { createInitialState } from "../src/game-core.mjs";import { createGirlfriendFromProfile } from "../src/girlfriend-manager.mjs";import { GAME_MODES } from "../src/scenario-state.mjs";import { selectNextStoryScene } from "../src/story-manager.mjs";import { DAY20_COST_CHOICES,DAY20_FINISH_CHOICES,DAY20_MENU_CHOICES,applyLockedDay20ChoiceState,getLockedDay20ResumePresentation,getLockedDay20Segment,validateLockedDay20Runtime } from "../src/day20-campaign-runtime.mjs";
const s=createInitialState(createGirlfriendFromProfile("haeun",()=>.5),()=>.5,{mode:GAME_MODES.MARRIAGE_30});s.day=20;s.storyHistory=[{sceneId:"m30-day19-current-shared-chore",choiceId:"chore19_renegotiate_stop",day:19,response:"완료"}];s.storyFlags={day20CurrentSharedMealPending:true};s.scenario.featureUnlocks={finance:true,shop:true};assert.equal(selectNextStoryScene(s)?.id,"m30-day20-current-shared-meal");assert.equal(validateLockedDay20Runtime(),true);assert.equal(getLockedDay20Segment(s).filter(x=>x.type==="choice").length,1);assert.equal(applyLockedDay20ChoiceState(s,DAY20_MENU_CHOICES[0].id).stage,1);assert.equal(getLockedDay20ResumePresentation(s).backgroundId,"home-morning");assert.equal(applyLockedDay20ChoiceState(s,DAY20_COST_CHOICES[0].id).stage,2);assert.equal(applyLockedDay20ChoiceState(s,DAY20_FINISH_CHOICES[0].id).stage,3);assert.equal(s.storyFlags.day20CurrentSharedMealCompleted,true);assert.equal(s.storyFlags.day21CurrentFullWorkdayPending,true);assert.equal(s.scenario.featureUnlocks.investment,undefined);for(const id of ["current-shared-meal-plan","shared-meal-cost-boundary","meal-stop-and-leftover-consent"])assert.ok(s.scenario.unlockedActions.includes(id));assert.ok(s.scenario.followUpHooks.includes("day21-current-full-workday"));assert.ok(s.scenario.clues.includes("current-shared-meal-record"));const restored=structuredClone(s);assert.equal(restored.storyFlags.day20RuntimeStage,3);assert.equal(getLockedDay20Segment(restored).at(-1).type,"sceneEnd");console.log("day20-runtime.test: all assertions passed");
