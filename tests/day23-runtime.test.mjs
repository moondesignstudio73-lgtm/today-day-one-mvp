import assert from "node:assert/strict";import { createInitialState } from "../src/game-core.mjs";import { createGirlfriendFromProfile } from "../src/girlfriend-manager.mjs";import { GAME_MODES } from "../src/scenario-state.mjs";import { selectNextStoryScene } from "../src/story-manager.mjs";import { DAY23_FUTURE_CHOICES,DAY23_SHARE_CHOICES,DAY23_VERIFY_CHOICES,applyLockedDay23ChoiceState,getLockedDay23ResumePresentation,getLockedDay23Segment,validateLockedDay23Runtime } from "../src/day23-campaign-runtime.mjs";
const s=createInitialState(createGirlfriendFromProfile("haeun",()=>.5),()=>.5,{mode:GAME_MODES.MARRIAGE_30});s.day=23;s.storyHistory=[{sceneId:"m30-day22-current-recovery-day",choiceId:"recovery22_activity_stop_early",day:22,response:"완료"}];s.storyFlags={day23CurrentFamilyContactPending:true};s.scenario.featureUnlocks={finance:true,shop:true};assert.equal(selectNextStoryScene(s)?.id,"m30-day23-current-family-contact");assert.equal(validateLockedDay23Runtime(),true);assert.equal(getLockedDay23Segment(s).filter(x=>x.type==="choice").length,1);assert.equal(applyLockedDay23ChoiceState(s,DAY23_VERIFY_CHOICES[0].id).stage,1);assert.equal(getLockedDay23ResumePresentation(s).backgroundId,"home-morning");assert.equal(applyLockedDay23ChoiceState(s,DAY23_SHARE_CHOICES[0].id).stage,2);assert.equal(applyLockedDay23ChoiceState(s,DAY23_FUTURE_CHOICES[0].id).stage,3);assert.equal(s.storyFlags.day23CurrentFamilyContactCompleted,true);assert.equal(s.storyFlags.day24CurrentCommitmentCheckPending,true);assert.equal(s.scenario.featureUnlocks.investment,undefined);for(const id of ["verified-current-family-contact","family-information-boundary","family-contact-consent-and-exit"])assert.ok(s.scenario.unlockedActions.includes(id));assert.ok(s.scenario.followUpHooks.includes("day24-current-commitment-check"));assert.ok(s.scenario.clues.includes("current-family-contact-record"));const restored=structuredClone(s);assert.equal(restored.storyFlags.day23RuntimeStage,3);assert.equal(getLockedDay23Segment(restored).at(-1).type,"sceneEnd");console.log("day23-runtime.test: all assertions passed");
