import assert from "node:assert/strict";import { createInitialState } from "../src/game-core.mjs";import { createGirlfriendFromProfile } from "../src/girlfriend-manager.mjs";import { GAME_MODES } from "../src/scenario-state.mjs";import { selectNextStoryScene } from "../src/story-manager.mjs";import { DAY26_DOCUMENT_CHOICES,DAY26_EFFECT_CHOICES,DAY26_SIGNATURE_CHOICES,applyLockedDay26ChoiceState,getLockedDay26ResumePresentation,getLockedDay26Segment,validateLockedDay26Runtime } from "../src/day26-campaign-runtime.mjs";
const s=createInitialState(createGirlfriendFromProfile("haeun",()=>.5),()=>.5,{mode:GAME_MODES.MARRIAGE_30});s.day=26;s.storyHistory=[{sceneId:"m30-day25-current-wedding-scope",choiceId:"wedding25_guest_each_consent",day:25,response:"완료"}];s.storyFlags={day26CurrentLegalPreparationPending:true};s.scenario.featureUnlocks={finance:true,shop:true};assert.equal(selectNextStoryScene(s)?.id,"m30-day26-current-legal-preparation");assert.equal(validateLockedDay26Runtime(),true);assert.equal(getLockedDay26Segment(s).filter(x=>x.type==="choice").length,1);assert.equal(applyLockedDay26ChoiceState(s,DAY26_DOCUMENT_CHOICES[0].id).stage,1);assert.equal(getLockedDay26ResumePresentation(s).backgroundId,"home-morning");assert.equal(applyLockedDay26ChoiceState(s,DAY26_EFFECT_CHOICES[0].id).stage,2);assert.equal(applyLockedDay26ChoiceState(s,DAY26_SIGNATURE_CHOICES[0].id).stage,3);assert.equal(s.storyFlags.day26CurrentLegalPreparationCompleted,true);assert.equal(s.storyFlags.day27CurrentFinalCheckPending,true);assert.equal(s.scenario.featureUnlocks.investment,undefined);for(const id of ["current-legal-document-minimum","marriage-legal-effects-boundary","individual-signature-and-stop-right"])assert.ok(s.scenario.unlockedActions.includes(id));assert.ok(s.scenario.followUpHooks.includes("day27-current-final-check"));assert.ok(s.scenario.clues.includes("current-legal-preparation-record"));const restored=structuredClone(s);assert.equal(restored.storyFlags.day26RuntimeStage,3);assert.equal(getLockedDay26Segment(restored).at(-1).type,"sceneEnd");console.log("day26-runtime.test: all assertions passed");
