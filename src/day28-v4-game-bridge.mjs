import {BACKGROUND_ASSETS,getNpcSprite} from './assets/asset-manifest.mjs?v=25';
import {MAP_LOCATION_ASSETS} from './map-location-assets.mjs';
import {applyDay28V4Choice,beginDay28V4,completeDay28V4,getDay28V4Entry,resolveDay28V4Contact,resolveDay28V4HomeInvitation,resolveDay28V4Meeting,resolveDay28V4NewRelationship,resolveDay28V4NextMeeting,resolveDay28V4Relationship,validateDay28V4} from './day28-v4-state-contract.mjs?v=2';
import {getDay28V4PlayableOpening} from './day28-v4-playable-opening.mjs';
import {getDay28V4PlayableMiddle} from './day28-v4-playable-middle.mjs?v=2';
import {getDay28V4PlayableEnding} from './day28-v4-playable-ending.mjs';
import {STORY_OUTFIT_ASSETS} from './story-outfit-assets.mjs';

export const DAY28_V4_CAMPAIGN_SLOT='m30-day28-current-ceremony-rehearsal';
const boundaries=new Set(['day28Boundary']);
const resolutionTypes=new Set(['day28MeetingCue','day28RelationshipCue','day28ContactCue','day28HomeInvitationCue','day28NextMeetingCue','day28NewRelationshipCue']);
const clock=Object.freeze({morning:'08:00',daytime:'15:00',afternoon:'17:00',evening:'19:00',night:'22:00'});
const backgroundMap=Object.freeze({'memory-park':'neighborhood-park-day',cafe:'small-cafe','haeun-home':'girlfriend-home'});
const npcMap=Object.freeze({yuri:'player-ex',seojin:'female-coworker',ara:'female-friend'});

export function getDay28V4GameCompatibility(state){const entry=getDay28V4Entry(state);if(entry.mode==='BLOCKED_PREREQUISITE'&&state.storyFlags?.day27RuntimeComplete===true&&state.storyFlags?.day27V4?.complete!==true)return {mode:'LEGACY'};return entry;}
export function prepareDay28V4GameEntry(state){const entry=getDay28V4GameCompatibility(state);return entry.mode==='V4_NEW'?beginDay28V4(state):entry;}
function presentation(direction){let backgroundId=backgroundMap[direction.location]??direction.location;if(direction.location==='home')backgroundId=direction.time==='night'?'home-night':'home-evening';const backgroundUrl=BACKGROUND_ASSETS[backgroundId]??MAP_LOCATION_ASSETS[backgroundId];if(!backgroundUrl)throw new Error(`DAY28_BACKGROUND_MISSING:${backgroundId}`);const ids=direction.characters??[],characterId=ids.includes('girlfriend')?'girlfriend':ids[0]??null,characterAssetUrl=characterId==='girlfriend'?STORY_OUTFIT_ASSETS.day12:characterId?getNpcSprite(npcMap[characterId]??characterId):null;return {backgroundId,backgroundUrl,characterId,characterAssetUrl,expressionId:'calm',poseId:'standing',timeOfDay:direction.time,storyClock:clock[direction.time]??'22:00',storyLocation:direction.location};}
function mapSteps(steps){return steps.filter(step=>!boundaries.has(step.type)).map(step=>step.type==='sceneDirection'?{type:'transition',style:'crossfade',label:`SCENE ${String(step.number).padStart(2,'0')} · ${step.title}`,sceneNumber:step.number,bgmId:step.time==='night'?'theme':'daily',...presentation(step)}:step);}
const openingPhases=new Set(['morning','meeting_resolution','meeting_start','heard_meaning','answer_feeling','small_promise','changed_path','after_laugh','relationship_wish','relationship_resolution']);
const middlePhases=new Set(['breakup_close','breakup_belongings','continuing_good_feeling','continuing_distance','contact_resolution','continuing_intimacy','daily_listening','home_invitation_resolution','home_pause','continuing_farewell','next_meeting_resolution']);
function rawSegment(chapter){if(openingPhases.has(chapter.phase))return getDay28V4PlayableOpening(chapter);if(middlePhases.has(chapter.phase))return getDay28V4PlayableMiddle(chapter);return getDay28V4PlayableEnding(chapter);}
export function getDay28V4GameSegment(state){const chapter=state.storyFlags?.day28V4;if(!validateDay28V4(chapter))throw new Error('DAY28_INVALID_SAVE');return mapSteps(rawSegment(chapter));}
function resumeDirection(chapter){if(chapter.phase==='morning'||chapter.phase==='meeting_resolution')return {location:'home',time:'morning',characters:[]};if(['meeting_start','heard_meaning','answer_feeling','small_promise','changed_path','after_laugh','relationship_wish','relationship_resolution','breakup_close','breakup_belongings','continuing_good_feeling','continuing_distance','contact_resolution','continuing_intimacy'].includes(chapter.phase))return {location:chapter.facts.meetingMethod==='CALL'?'home':'memory-park',time:'daytime',characters:chapter.facts.meetingTarget==='HAEUN'?['girlfriend']:[chapter.facts.meetingTarget?.toLowerCase()].filter(Boolean)};if(chapter.phase==='new_meeting_wish'||chapter.phase==='new_relationship_resolution')return {location:'cafe',time:'daytime',characters:[chapter.input.newMeeting.recipient.toLowerCase()]};if(chapter.phase.startsWith('solo_'))return {location:chapter.phase==='solo_afternoon'?'cafe':'home',time:chapter.phase==='solo_afternoon'?'afternoon':'evening',characters:[]};if(chapter.phase==='home_pause')return {location:chapter.facts.homeInvitation==='HAEUN_HOME'?'haeun-home':'home',time:'evening',characters:['girlfriend']};return {location:'home',time:'night',characters:[]};}
export function getDay28V4GameResumePresentation(state){const chapter=state.storyFlags?.day28V4;if(!validateDay28V4(chapter))throw new Error('DAY28_INVALID_SAVE');return presentation(resumeDirection(chapter));}
function snapshot(state){return {chapter:structuredClone(state.storyFlags?.day28V4),history:structuredClone(state.storyHistory??[]),pending:state.pendingStoryId};}
function restore(state,saved){state.storyFlags.day28V4=saved.chapter;state.storyHistory=saved.history;state.pendingStoryId=saved.pending;}
function postTransition(previous,chapter){if(previous==='breakup_belongings'||previous==='continuing_farewell'||previous==='next_meeting_resolution')return mapSteps([...getDay28V4PlayableMiddle(chapter),...getDay28V4PlayableEnding(chapter)]);return mapSteps(rawSegment(chapter));}
export function applyDay28V4GameChoice(state,id){const saved=snapshot(state),previous=state.storyFlags.day28V4.phase;try{const result=applyDay28V4Choice(state,id);return {result,steps:postTransition(previous,state.storyFlags.day28V4)};}catch(error){restore(state,saved);throw error;}}
export function applyDay28V4GameResolution(state,response){const saved=snapshot(state),previous=state.storyFlags.day28V4.phase;try{const result=response?.type==='day28MeetingResponse'?resolveDay28V4Meeting(state,response):response?.type==='day28RelationshipResponse'?resolveDay28V4Relationship(state,response):response?.type==='day28ContactResponse'?resolveDay28V4Contact(state,response):response?.type==='day28HomeInvitationResponse'?resolveDay28V4HomeInvitation(state,response):response?.type==='day28NextMeetingResponse'?resolveDay28V4NextMeeting(state,response):response?.type==='day28NewRelationshipResponse'?resolveDay28V4NewRelationship(state,response):(()=>{throw new Error('DAY28_UNKNOWN_RESOLUTION')})();return {result,steps:postTransition(previous,state.storyFlags.day28V4)};}catch(error){restore(state,saved);throw error;}}
export function completeDay28V4GameChapter(state,cue){const chapter=completeDay28V4(state,cue);state.storyHistory??=[];if(!state.storyHistory.some(record=>record.sceneId===DAY28_V4_CAMPAIGN_SLOT))state.storyHistory.push({sceneId:DAY28_V4_CAMPAIGN_SLOT,scenarioId:'day28-notion-v4',day:28,arc:'다시 만나자는 뜻',choiceId:chapter.choices.filter(record=>record.kind==='choice').at(-1)?.id??null,response:chapter.facts.relationshipState==='END'?'오늘 정한 끝과 거리를 다시 확인할 연락 없이 지켰다.':chapter.facts.relationshipState==='CONTINUE'||chapter.facts.relationshipState==='KNOW_AGAIN'?'어제로 돌아가지 않고 오늘 가능한 관계와 거리를 다시 골랐다.':'모르는 답을 만들어 내지 않고 내 생활과 내일을 남겼다.',facts:structuredClone(chapter.facts),choices:structuredClone(chapter.choices),route:chapter.facts.route});state.storyFlags.day28RuntimeComplete=true;state.pendingStoryId=null;return {type:'sceneEnd',day:28,complete:true};}
export const isDay28V4ResolutionStep=step=>resolutionTypes.has(step?.type);
